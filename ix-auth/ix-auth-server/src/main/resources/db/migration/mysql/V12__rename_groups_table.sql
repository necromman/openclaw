-- MySQL 8 방언 (mariadb/ 사본 — 다른 곳은 파일 안 주석으로 표시했다)
-- groups → ixauth_groups 개명 — **MySQL 에서는 할 일이 없다.**
--
-- =====================================================================
-- 이 파일이 비어 있는 것이 요점이다
-- =====================================================================
--
-- PostgreSQL·MariaDB 에는 `groups` 로 만들어진 설치본이 이미 있어 V12 가 RENAME 을 한다.
-- MySQL 은 이 폴더가 생긴 지금이 첫 설치라 옮길 표가 없다 — V1 이 처음부터
-- ixauth_groups 를 만든다(mysql/V1__baseline.sql ②).
--
-- 그래도 파일을 두는 이유는 **방언 사본이 함께 늙게** 하기 위해서다. 세 폴더의 버전
-- 번호가 어긋나기 시작하면 "postgresql 의 V13 은 mysql 의 몇 번인가" 를 매번 따져야 하고,
-- 그 계산이 한 번 틀리면 조용히 빠진 마이그레이션이 생긴다.
-- 번호를 맞춰 두면 개수 비교만으로 누락이 드러난다(MigrationParityTest).
--
-- Flyway 는 실행할 문장이 없는 마이그레이션도 정상으로 보고 히스토리에 기록한다.
