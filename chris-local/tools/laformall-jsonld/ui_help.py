"""도움말 탭. 필수·선택 항목 표와 절차 안내."""

from __future__ import annotations

import webbrowser

import fields

HELP_TEXT = """화면 보는 법

- 위쪽 띠에 1단계부터 5단계까지 있습니다. 끝난 단계는 초록, 지금 단계는 파랑,
  남은 단계는 회색입니다. 다음에 누를 버튼은 파란 버튼으로 표시됩니다.
- 왼쪽은 단계 카드와 상품 목록, 오른쪽은 상품 하나를 채우는 입력 폼입니다.
  가운데 경계선을 끌면 넓이를 바꿀 수 있습니다.
- 창 아래에 진행 표시줄이 있습니다. 시간이 걸리는 일은 몇 건 중 몇 건인지,
  지금 무엇을 하는지 보여 주고 "취소" 로 멈출 수 있습니다.
- 목록의 열은 이렇게 읽습니다.
  입력 상태: 이 프로그램 폼에 값이 얼마나 찼는지("필수 6개 남음" 처럼)
  페이지 적용: 실제 상품 페이지에 정보표가 붙어 있는지(적용됨·미적용·가격 불일치)
  구글 테스트: 구글 리치 결과 테스트 결과
  행 색은 "페이지 적용" 기준입니다. 셀에 마우스를 올리면 자세한 내용이 나옵니다.

1단계. 상품 불러오기

- "관리자 엑셀 가져오기" 가 가장 정확합니다. 고도몰 관리자 > 상품 관리 >
  상품 목록 > 엑셀다운로드 로 받은 파일을 고르면 상품코드·상품명·판매가·
  노출상태·판매상태를 그대로 읽어 옵니다. 미노출 상품은 회색 취소선으로
  표시하고 선택에서 빼 둡니다.
- "판매 중 상품 불러오기" 는 메인·카테고리 목록·전체 검색을 실제로 받아
  지금 진열된 상품만 모읍니다.
- "사이트맵에서 불러오기" 는 올라가 있는 sitemap.xml 을 읽습니다. 파일의
  기준 날짜와 상품 수를 알려 주니 최근에 올린 것이면 그대로 써도 됩니다.

2단계. 정보 가져오기

- 상품명·판매가·정가·이미지·재고·제조사·원산지를 페이지에서 읽어 옵니다.
- 페이지에 이미 JSON-LD 정보표가 있으면 그 값(설명·이미지·가격·브랜드·판매자
  등)까지 폼에 채우고 "페이지 적용" 을 함께 판정합니다.
- 설명·재질·크기·무게·색상은 페이지에 글자로 없어서 사람이 채웁니다.
  폼에서 "직접 입력" 배지가 붙은 항목입니다.

3단계. 항목 입력

- 목록에서 행을 누르면 오른쪽 폼에 그 상품이 뜹니다. 별표가 붙은 항목을 다 채웁니다.
- 회색 글씨는 예시이고 칸을 누르면 사라집니다. 검은 글씨가 실제로 들어간 값입니다.
- 처음이면 "예시로 채우기" 를 눌러 어떤 값이 들어가는지 보고 실제 값으로 고칩니다.
- 대표 이미지는 "이미지 고르기" 를 누르면 이 상품 사진을 썸네일로 보여 줍니다.
  종류(대표·목록·확대·상세)와 픽셀 크기가 함께 나오니 큰 것을 고르면 됩니다.
  주소가 godomall.speedycdn.net 인 것은 고도몰이 이미지를 자체 서버에 두기
  때문이고 정상입니다. 구글은 그 주소가 열리는지만 봅니다.
- 설명은 60~160자를 권장합니다. 칸이 좁으면 "칸 늘리기" 로 넓힙니다.
- 다 쓰면 "행에 저장" 을 누릅니다.

4단계. 스니펫 생성·복사

- "스니펫 생성" 을 누릅니다. 필수 항목이 비면 어떤 항목인지 알려 주고 멈춥니다.
- 규제 문구가 걸리면 위반 단어를 빨갛게 표시하고 대체 문안을 보여 줍니다.
- "개별 복사"·"전체 복사" 로 복사하고, 파일로 남기려면 "파일 저장" 을 누릅니다.
- 고도몰 관리자 > 상품 관리 > 상품 수정 > 상품 상세설명 을 열고 에디터를
  HTML 편집 모드로 바꾼 뒤 맨 아래에 붙여넣고 저장합니다.

5단계. 검증

- 고도몰에 붙여넣고 저장한 뒤 초록 버튼 "붙여넣기 끝났으면 다시 불러와 검증"
  을 누릅니다. 상품을 처음처럼 다시 불러와 표의 "페이지 적용" 열을 갱신하고
  "적용됨 n개, 미적용 m개, 가격 불일치 k개" 를 알려 줍니다. 미적용에서 적용으로
  바뀐 행은 초록으로 강조되고 "새로 적용" 배지가 붙습니다. 프로그램을 다시 켜고
  확인할 때는 화면 맨 위 띠의 "적용 결과 다시 확인" 을 눌러도 같습니다.
  회차마다 기록이 남아 이력 탭 아래 "검증 회차" 표에서 추이를 볼 수 있습니다.
- "적용 확인" 은 지금 표에 있는 상품만 다시 받아 정보표가 붙었는지, 가격이
  화면과 같은지 봅니다.
- "구글 일괄 테스트" 가 최종 판정입니다. 상품마다 브라우저를 띄워
  "유효한 항목 N개" 를 읽어 옵니다. 진행 중에는 목록의 구글 테스트 열에
  "브라우저 여는 중 / 페이지 로딩 / 결과 대기(N초) / 결과 읽음" 이 보입니다.
  취소하면 이미 끝난 것은 남고 나머지는 "취소됨" 이 됩니다. 같은 버튼을 다시
  누르면 취소된 것부터 이어서 합니다.
- "구글 테스트 창 열기" 는 결과 화면을 창으로 띄워 직접 보여 줍니다.

도구

- "사이트맵 만들기" 는 지금 노출 상품으로 sitemap.xml 을 만듭니다. 고도몰은
  사이트맵을 자동으로 갱신하지 않으니 상품이 바뀌면 다시 만들어 올립니다.
  올리는 곳은 고도몰 관리자 > 상점관리 > 검색엔진 최적화(SEO) 의 사이트맵
  업로드이고, 그 항목이 없으면 디자인 > 디자인 관리에서 스킨 루트에 올립니다.
  그 다음 robots.txt 에 "Sitemap: https://cstpillow.com/sitemap.xml" 을 넣고
  구글 서치 콘솔·네이버 서치어드바이저에 주소를 제출합니다.
- "llms.txt 만들기" 는 효과가 검증되지 않은 부가 항목입니다. 사이트맵 다음에
  여력이 있을 때 만들어 사이트 루트에 올립니다.

로컬 판정과 구글 테스트

- 입력 상태(로컬 판정)는 이 프로그램이 즉시 내는 참고 판정입니다.
- 최종 판정은 구글 리치 결과 테스트입니다. 입력이 다 찼어도 구글 결과가
  "미감지" 면 붙여넣기나 스킨 설정에 문제가 있다는 뜻입니다.

설치할 것은 없습니다

- 실행 파일 하나로 돌아갑니다. 파이썬 같은 것을 따로 깔지 않습니다.
- 구글 자동 테스트만 이 PC 에 Edge 또는 Chrome 이 설치돼 있어야 합니다.
  창은 화면 밖에서 돌아 방해하지 않습니다. 둘 다 없으면 "브라우저 없음" 으로
  표시하고 나머지 기능은 그대로 됩니다.

주의

- JSON-LD 의 가격이 화면 가격과 다르면 표시광고 위반이 됩니다.
  가격을 바꾸면 스니펫도 다시 만들어 붙입니다.
- 화면에 없는 별점·후기 수를 넣지 않습니다. aggregateRating 은
  페이지에 실제로 보이는 값이 있을 때만 켭니다.
- 대표 이미지는 상품의 대표 사진 한 장입니다. 폼의 "이미지 고르기" 를 누르면
  이 상품 사진을 썸네일로 보여 주니 골라서 넣으면 됩니다. 주소가
  godomall.speedycdn.net 으로 시작하는 것은 고도몰이 이미지를 자체 서버에
  두기 때문이고 정상입니다. 구글은 그 주소가 열리는지만 봅니다.
  가로 1200px 이상이면 가장 좋고, 작으면 경고로 알려 줍니다.
- 공통 헤더·푸터에 넣지 않습니다. 상품마다 상품 상세설명에만 넣습니다.
  전 상품 한 번에 넣으려면 스킨 goods_view.htm 에 치환코드로 심는 방법을
  따로 검토합니다(이 프로그램은 상품별 스니펫만 만듭니다).
- 설명에 치료·완치·예방·교정·통증 완화·혈액순환 같은 표현을 쓰지 않습니다.
  베개·마사지기는 의료기기 허가를 받지 않은 공산품이고, 이런 표현은
  의료기기 오인 광고로 평가됩니다. 물리 사양과 사용 방법으로 씁니다.
- 이 프로그램의 문구 검사는 사전 대조일 뿐이고 적법성을 보증하지 않습니다.
  최종 판단은 담당자와 법률 검토의 몫입니다.

개발자 메모

- CLI: laformall-jsonld.exe --cli --out <폴더> <URL 또는 goodsNo ...>
       설명 매핑은 --desc-file desc.csv (goodsNo,설명)
- 자기검사: laformall-jsonld.exe --selftest --out <폴더>
- 설정·이력 파일: %APPDATA%\\laformall-jsonld\\
"""


def build_help_tab(app, frame) -> None:
    tk, ttk = app.tk, app.ttk
    top = ttk.Frame(frame)
    top.pack(fill="x", padx=6, pady=6)
    ttk.Button(
        top,
        text="구글 리치 결과 테스트 열기",
        command=lambda: webbrowser.open("https://search.google.com/test/rich-results"),
    ).pack(side="left")
    ttk.Button(
        top,
        text="schema.org 검사기 열기",
        command=lambda: webbrowser.open("https://validator.schema.org/"),
    ).pack(side="left", padx=4)
    ttk.Button(
        top,
        text="라포르몰 열기",
        command=lambda: webbrowser.open(app.cfg["domain"]),
    ).pack(side="left")
    text = tk.Text(frame, wrap="word", font=("Segoe UI", 9))
    vsb = ttk.Scrollbar(frame, orient="vertical", command=text.yview)
    text.configure(yscrollcommand=vsb.set)
    text.pack(side="left", fill="both", expand=True, padx=(6, 0), pady=6)
    vsb.pack(side="left", fill="y", pady=6)
    text.tag_configure("h", font=("Segoe UI", 11, "bold"))
    text.tag_configure("req", foreground="#c00000", font=("Segoe UI", 9, "bold"))
    text.tag_configure("opt", foreground="#205080", font=("Segoe UI", 9, "bold"))
    text.insert("end", "필수 항목 표\n", "h")
    text.insert(
        "end",
        "아래 항목은 모두 채워야 스니펫이 생성됩니다. 정보가 많을수록 AI 답변에 인용될 여지가 커집니다.\n\n",
    )
    for label, example, hint in fields.required_table():
        text.insert("end", f"  {label}\n", "req")
        text.insert("end", f"    예시: {example}\n    이유: {hint}\n")
    text.insert("end", "\n선택 항목 표\n", "h")
    text.insert("end", "비워 두면 스니펫에서 빠집니다. 구글 권장 속성이 여기에 있습니다.\n\n")
    for label, example, hint in fields.optional_table():
        text.insert("end", f"  {label}\n", "opt")
        text.insert("end", f"    예시: {example}\n    이유: {hint}\n")
    text.insert("end", "\n" + HELP_TEXT)
    text.configure(state="disabled")
