# DELIVERY-PLAN.md - 진바이오테크 납품 기능 계획 (D~I 단계)

> 2026-09-07 제안서(`진바이오테크-제안서-20260907.docx`)와 사용자 지시(로그인 = 기기 자동 인증, 로그아웃, 관리자의 사용자 관리, 관리자·임원·직원 구분, PDF·MS Office 미리보기, hwp 제외)를 현재 포크와 대조해 남은 작업을 단계로 나눈 정본이다.\
> 앞선 A·B·C 단계 기록은 [AUTH-PLAN.md](AUTH-PLAN.md) 끝의 "A·B·C 진행 기록", 인수인계는 [HANDOFF.md](HANDOFF.md).

작성 2026-09-07 (KST). 기준 커밋 `chris/main@3959ade8bb6`.

---

## 0. 실측으로 확인한 것 (2026-09-07 20:30~21:10)

| 항목                     | 결과                                                                                                                                                                                                 | 근거                                                                                                                                                                           |
| ------------------------ | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| 로그인 후 기기 자동 인증 | **이미 동작.** compose 스택(18800) 새 브라우저 컨텍스트에서 로그인 즉시 `/chat/main` 진입, 게이트웨이 로그 `[ws] authenticated user connected user=admin@deploy.local`, 페어링 요청·기기 토큰 미생성 | `src/gateway/server/ws-connection/connect-policy.ts:39-46` (`identity-session`), 지역성 조건 없음                                                                              |
| 로그아웃                 | BFF `POST /auth/logout` 은 있음. **UI 신원 메뉴에 항목 없음.** 서버는 열린 WS 를 끊지 않고 화면 리로드에 의존                                                                                        | `src/gateway/ix-auth-http.ts:327-372`, `ui/src/features/ix-auth/ix-auth-session-api.ts:251-260`                                                                                |
| 관리자 사용자 관리       | 앱 안에는 초대 발급·가입 승인·부서 조회만(설정 > 연결). 목록·삭제·역할 변경은 `/admin/identity/` 로 중계되는 IX-Auth 콘솔인데 **별도 로그인 화면이 한 번 더 뜬다**                                   | `ui/src/pages/connection/ix-auth-invite-section.ts`, IX-Auth `UserAdminController` (목록·상세·생성·PATCH·삭제·password-reset·invite·unlock·mfa-reset·roles·sessions 전부 있음) |
| 역할                     | superadmin / admin / moderator / member 4단계. **임원 없음.** UI 는 역할 코드를 날것으로 표시                                                                                                        | `src/auth/ix-auth/ix-auth-role-map.ts:13-50`                                                                                                                                   |
| 문서 미리보기            | 로컬 WSL 은 soffice 있음. **납품 컨테이너에 soffice 없음** → PDF 정상, docx·xlsx 는 이미지 없는 HTML 폴백, pptx·doc·xls·ppt 는 실패. 안내문 영어뿐                                                   | `chris-local/docker-compose.ixauth.yml:110` (폰트만), `src/gateway/document-convert.ts:110-121`                                                                                |
| 납품 스택 모델           | "사용 가능한 모델 없음". DEPLOY.md 에 모델·API 키 항목 없음                                                                                                                                          | 라이브 화면, `chris-local/DEPLOY.md`                                                                                                                                           |
| 기본 에이전트 미지정     | `cron.list` → "Agent-less cron job has no resolvable owner", `talk.catalog` → `AGENT_SELECTION_REQUIRED`                                                                                             | 게이트웨이 로그 20:36:07                                                                                                                                                       |

## 1. 제안서 요구 대비 현황

| 제안서 요구 (장)                               | 현재                                                                                                         | 결손                                                                                                                     | 단계                             |
| ---------------------------------------------- | ------------------------------------------------------------------------------------------------------------ | ------------------------------------------------------------------------------------------------------------------------ | -------------------------------- |
| 웹 화면, 사내 주소 접속 (3)                    | 완료                                                                                                         | -                                                                                                                        | -                                |
| 채팅(카카오톡·텔레그램처럼) (3)                | 텔레그램 확장 있음. 카카오톡 확장 없음                                                                       | 텔레그램 발신자와 IX-Auth 계정 연결 장치 없음. 부서 경계는 에이전트 바인딩으로만 적용                                    | 문서화(I), 연결은 후속           |
| 자료 찾기·근거 인용 (2·3·6)                    | `memory.search.extraPaths` 로 워크스페이스 밖 폴더 인덱스 가능, 인용 `path#L12-L30` 지원                     | **인덱서가 `.md` 만 수집.** pdf·docx·xlsx·pptx 는 인덱스 대상이 아님. 한글 FTS 토크나이저 기본값 부적합                  | **I** (대), D(설정)              |
| 문서 초안·요약 (3)                             | 에이전트 기본 능력                                                                                           | -                                                                                                                        | -                                |
| 반복 확인·알림 (3)                             | cron 5종 + 텔레그램 전달 + 타임존                                                                            | 생성자 정보가 공개 응답에서 제거됨(관리자가 누가 만든 스케줄인지 못 봄)                                                  | G(소)                            |
| 읽기 전용 원칙 (3)                             | 세션 `permissionMode: read-only` 와 도구 deny 목록으로 가능                                                  | **config 로 "이 에이전트는 항상 읽기 전용" 선언 불가.** `readonly` 도구 프로필 없음. `tools.fs.workspaceOnly` 기본 false | **G** (중)                       |
| 폴더 단위 접근 범위 (4)                        | 에이전트 1개 = 워크스페이스 루트 1개                                                                         | 다중 폴더 허용목록 없음. 부서 = 에이전트 = 폴더 구조로 대응                                                              | G(문서·설정), allowRoots 는 후속 |
| 사람마다 다른 자료 범위 (4)                    | 부서 경계(B 단계) + 역할 상한                                                                                | **임원 역할 없음**                                                                                                       | **E**                            |
| 누가 무엇을 물었고 어떤 자료를 봤는지 기록 (4) | `audit_events` 는 메타데이터 전용. actor 가 사람이 아님. 질문 본문·파일 경로·로그인 미기록. 30일/10만행 고정 | **AUTH-PLAN M4 전량 미착수**                                                                                             | **H** (대)                       |
| 외부 접속 개폐 (4)                             | bind 5모드 + `controlUi.enabled` + 단일 포트                                                                 | - (DEPLOY.md 7절)                                                                                                        | -                                |
| NAS 안 모델 (4·5)                              | ollama 등 6종, 임베딩까지 사내 완결                                                                          | DEPLOY.md 에 모델 설정 절차 없음                                                                                         | D(문서)                          |
| 로그인 = 기기 인증 (지시)                      | 완료                                                                                                         | 로그아웃 시 서버측 WS 종료 없음                                                                                          | D                                |
| 로그아웃 (지시)                                | API 만                                                                                                       | 메뉴 항목                                                                                                                | **D**                            |
| 관리자 사용자 관리 (지시)                      | IX-Auth 콘솔(이중 로그인)                                                                                    | 앱 내 화면                                                                                                               | **F**                            |
| 관리자·임원·직원 (지시)                        | 4역할, 임원 없음                                                                                             | 임원 역할 + 한국어 역할명                                                                                                | **E**                            |
| PDF·MS Office 미리보기 (지시)                  | 로컬만 완전                                                                                                  | 납품 이미지에 soffice 없음, 안내문 영어                                                                                  | **D**                            |

## 2. 감독이 정한 기본값 (사용자가 바꿀 수 있음)

| 항목                             | 채택                                                                                                                                                                                                                                                               | 이유 · 대안                                                                                                                                                                                                                        |
| -------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 임원 역할 구현 방식              | IX-Auth `EXECUTIVE` → 게이트웨이 `executive`. 세션 타인 열람 상한 `view`, 스코프는 member 와 같음. **전 부서 열람은 부서 경계 코드를 고치지 않고 IX-Auth 에서 전 `dept-*` 그룹에 소속시켜 얻는다.** 초대 화면에서 역할이 임원이면 부서를 전체 선택으로 기본 채운다 | 부서 경계에 "전 부서 읽기 전용" 상태를 새로 넣으면 약 36개 파일 재설계·횡단 유출 위험(캐시 키). "부서는 상한을 넓히지 않고 좁히기만 한다" 원칙을 그대로 쓰는 편이 안전하다. 대안: 후속 단계에서 `crossDepartmentViewRoles` 별도 키 |
| 역할 한국어 표기                 | superadmin 시스템 관리자 / admin 관리자 / executive 임원 / moderator 중재자 / member 직원                                                                                                                                                                          | 초대·사용자 관리 화면의 선택지는 직원·임원·관리자 3개(+ 시스템 관리자는 superadmin 만). moderator 는 설정 호환용으로 남기되 선택지에서 뺀다                                                                                        |
| 관리자가 임원을 초대할 수 있는가 | 가능                                                                                                                                                                                                                                                               | admin 은 자기 부서만 보지만 사용자 관리는 admin 의 책무. 다만 superadmin 승격은 기존대로 차단                                                                                                                                      |
| 로그아웃                         | 신원 메뉴 항목 + 계정 화면 버튼. 서버는 `/auth/logout` 에서 그 프로필의 열린 WS 를 끊는다                                                                                                                                                                          | 클라이언트 리로드 의존 제거                                                                                                                                                                                                        |
| 사용자 관리 화면 위치            | 신원 메뉴 "사용자 관리" → `/settings/users` (superadmin·admin 만 링크·라우트)                                                                                                                                                                                      | 라우트 추가 비용은 있으나 설정 페이지 안의 블록으로는 목록·상세를 담기 어렵다. IX-Auth 콘솔 링크는 superadmin 에게만 "고급" 으로 남긴다                                                                                            |
| 미리보기 변환기                  | compose 빌드 인자에 `libreoffice-writer libreoffice-calc libreoffice-impress fonts-nanum` 추가                                                                                                                                                                     | 이미지 약 +580MiB. FILE-PREVIEW.md 예시와 일치시킨다. hwp 는 범위 밖(사용자 확정)                                                                                                                                                  |
| 읽기 전용                        | `readonly` 도구 프로필 신설 + `agents.entries.<id>.tools.permissionMode` 기본값 키. 납품 config 는 `tools.fs.workspaceOnly: true`                                                                                                                                  | deny 목록 수작업은 신규 도구 추가 시 누락 위험                                                                                                                                                                                     |
| 감사 원장 범위                   | 사람 귀속 원장 `audit_user_activity`: 로그인·로그아웃, 질문 본문(설정으로 on), 도구 파일 읽기 경로, 관리자 행위(초대·역할 변경·삭제), 세션 열람. 보존기간 설정 키. 조회 API·CLI·관리자 화면. **해시 체인은 넣지 않는다**(외부 SIEM 정본 권고 유지)                 | AUTH-PLAN M4 의 축소판. 계약서에 "내장 원장은 보존기간 내 조회용" 명시                                                                                                                                                             |
| 문서 인덱싱                      | 게이트웨이 안에 변환 단계를 두지 않고, **`openclaw knowledge sync` CLI + cron** 이 NAS 폴더의 pdf·docx·xlsx·pptx 를 `.md` 사이드카(원본 경로·수정시각 frontmatter)로 뽑아 인덱스 폴더에 쓴다. `extraPaths` 는 그 폴더를 가리킨다                                   | 인덱서(`memory-host-sdk`)를 건드리면 업스트림 리베이스 비용이 크다. 기존 `document-convert.ts`·`document-extract-html.ts`·`clawpdf` 재사용                                                                                         |
| 텔레그램                         | 부서당 봇 계정·에이전트 분리를 운영 규칙으로 문서화. 발신자-계정 연결은 후속                                                                                                                                                                                       | 코드 전수 확인 결과 연결 장치 없음                                                                                                                                                                                                 |

## 3. 단계

작업 규칙은 FORK.md 그대로: 브랜치 하나에 에이전트 하나, 게이트 `pnpm check` 0 실패 → 라이브 실측 → `origin/chris/main` 리베이스 → ff 머지 → 자동 배포 확인 → 브랜치 삭제. 빌드·테스트는 WSL `~/openclaw` 에서 브랜치를 체크아웃해 돌린다.

### D 단계 - 로그아웃·미리보기 변환기·납품 설정 기본값 (소)

브랜치 `chris/delivery-d`.

1. 신원 메뉴(사이드바 하단 사용자 메뉴)에 "로그아웃" 항목. ix-auth 모드에서만 렌더. 계정 화면에도 버튼. 완료 후 로그인 화면으로.
2. `/auth/logout` 처리에서 `disconnectClientsForUserProfile(profileId)` 호출 → 열린 WS 종료. 테스트 추가.
3. `docker-compose.ixauth.yml` 빌드 인자에 LibreOffice 3종 + `fonts-nanum`. 이미지 재빌드 후 docx·xlsx·pptx 미리보기 실측.
4. `documentPreview.*` 문구 한국어 7개(`ko` 로케일).
5. 납품 config(`ixauth-gateway-config/openclaw.json`): `tools.fs.workspaceOnly: true`, `memory.citations: "on"`, `memory.search.store.fts.tokenizer: "trigram"`, `agents.defaults.systemAgent.agentId`, `talk.agentId` → `cron.list`·`talk.catalog` 오류 제거.
6. DEPLOY.md: 모델 프로바이더 설정 절(외부 API 키 `.env` 자리표시자, ollama 사내 모델 절차), 재색인 절차(`openclaw memory index --force`), 미리보기 변환기 항목.
7. FILE-PREVIEW.md 와 compose 동기화 기록.

### E 단계 - 임원 역할 (중)

브랜치 `chris/delivery-e`.

1. `ix-auth-role-map.ts`: `EXECUTIVE: "executive"`, 정렬 admin 다음. 콘솔 역할 목록은 불변.
2. IX-Auth 마이그레이션 `V15__openclaw_executive_role.sql` 3방언 신규(V14 수정 금지). `build.gradle.kts` 버전 접미 `+openclaw.3`.
3. 배포 config·검증 스크립트 `roles.definitions.executive` (`sessions.others: "view"`, member 스코프).
4. 초대 표면: `IX_AUTH_INVITABLE_ROLE_CODES` 에 EXECUTIVE. 부서 다중 지정 지원(임원 기본 전체).
5. UI: 역할 한국어 표기 카탈로그 `ko-ix-auth.ts` 신설·등록, 초대 선택지 직원·임원·관리자(+시스템 관리자 조건부).
6. 테스트: `ix-auth-role-map.test.ts` 5역할, `department-access.test.ts` 는 executive 를 매트릭스에 넣지 않고 별도 describe(전 부서 그룹 소속 → 각 부서 open, 타인 세션 view 상한).
7. 라이브: 임원 계정 1명 초대 → 두 부서 세션 열람 가능·쓰기 불가 실측.
8. 문서: AUTH-IXAUTH.md 5절, AUTH-DEPARTMENTS.md 3절, DEPLOY.md 역할 표, `ix-auth/MODULE.md` 델타 표.

### F 단계 - 앱 내 사용자 관리 (중)

브랜치 `chris/delivery-f`.

1. BFF `/auth/admin/users` 계열: 목록(검색·페이지), 상세, 생성(=초대), 수정(이름·상태·부서), 역할 변경, 삭제(비활성 우선, 삭제는 superadmin), 비밀번호 재설정 링크, 초대 재발송, 잠금 해제, 세션 강제 종료. 인가는 세션 + superadmin·admin + CSRF, superadmin 승격·강등은 superadmin 만. 모든 행위를 감사 이벤트로 남길 훅 지점을 표시(H 단계에서 연결).
2. UI `/settings/users`: 목록·검색·상세 패널·역할/부서 변경·상태 토글·행위 버튼. 기존 초대·승인 블록을 이 화면으로 이동.
3. 신원 메뉴에 "사용자 관리"(superadmin·admin). IX-Auth 콘솔 링크는 superadmin 전용 "고급".
4. 테스트: BFF 표면 인가 매트릭스, UI 컴포넌트.
5. 라이브: 관리자 로그인 → 사용자 생성·역할 변경·비활성·재활성·삭제 실측. 이중 로그인 없음 확인.
6. 문서: AUTH-SIGNUP.md 5·6절 갱신 또는 `AUTH-USERS.md` 신설.

### G 단계 - 읽기 전용 프로필·폴더 범위·스케줄 생성자 (중)

브랜치 `chris/delivery-g`.

1. `readonly` 도구 프로필(`tool-catalog.ts`): read·memory_search·memory_get·web 조회류만. 신규 도구는 기본 제외.
2. `agents.entries.<id>.tools.permissionMode` 기본값 키(스키마·타입·세션 생성 시 적용). 세션이 이를 넘어 올리려면 ADMIN 스코프.
3. cron 공개 뷰에 생성자(프로필 id·이메일) 투영.
4. 납품 config 예시: 부서 에이전트 = NAS 부서 폴더 워크스페이스 + `readonly` + `workspaceOnly`. 인사·급여 폴더는 어느 에이전트에도 안 붙임.
5. 문서: `AUTH-DEPARTMENTS.md` 에 "폴더 단위 접근은 에이전트 워크스페이스로" 절, 텔레그램 운영 규칙(부서당 봇·에이전트).

### H 단계 - 사람 귀속 감사 원장 (대)

브랜치 `chris/delivery-h`.

1. 스키마 `audit_user_activity`(id, at, profile_id, email, role, departments, session_key, agent_id, kind, detail_json, ip, ua). kind: login·logout·prompt·tool_read·session_view·admin_action·file_download.
2. 기록 지점: `/auth/login`·`/auth/logout`, chat.send(본문은 `logging.audit.userActivity.promptText: true` 일 때), 도구 read·memory_get·sessions_files 열람(경로), 세션 열람(history 조회), F 단계 관리자 행위.
3. 보존 `logging.audit.userActivity.retentionDays`(기본 90) + 행 상한. 야간 정리.
4. 조회: RPC `audit.userActivity.list`(superadmin·admin, admin 은 자기 부서), CLI `openclaw audit users`, UI `/settings/audit` 필터(사람·기간·kind)·CSV 내보내기.
5. 테스트: 기록 지점별, 부서 필터, 보존 정리.
6. 문서: `AUTH-AUDIT.md` 신설(한계: 내장 원장은 조회용, SIEM 정본 권고).

### I 단계 - NAS 문서 → 마크다운 인덱스 (대)

브랜치 `chris/delivery-i`.

1. CLI `openclaw knowledge sync --source <dir> --out <dir> [--include ...]`: pdf(clawpdf 텍스트), docx·xlsx(`document-extract-html` → 텍스트), pptx(soffice → pdf → 텍스트). 산출 `.md` 에 frontmatter(원본 경로·크기·mtime·해시). 변경분만 재변환, 삭제 원본은 사이드카 제거.
2. cron 예시(`every 30m`)와 compose 볼륨 예시(NAS 마운트 읽기 전용).
3. 인용이 사이드카 경로를 가리키므로 frontmatter 의 원본 경로를 답변에 함께 내도록 `memory_search` 결과 장식 확인(가능하면 원본 경로로 치환).
4. 라이브: 샘플 폴더(pdf·docx·xlsx·pptx 각 1) → 검색 질의 → 출처 포함 답변 실측(한글).
5. 문서: `KNOWLEDGE.md` 신설(운영 절차·재색인·한계: hwp 제외).

## 3-1. NAS 설치·폴더 권한 대응 (2026-09-07 추가)

사용자 지시: 사용자·그룹별 폴더 접근 권한은 `chris-local/infra/local/openclaw-jinbio-nas-feasibility.md`(정본 chris-server) 의 결론대로 하고, 진바이오테크 NAS(Synology DS218+) 설치에 대응한다. 인프라 사본과 접속 절차는 [infra/README.md](infra/README.md).

채택 (문서의 "경로 A"):

- 부서·사람 = 에이전트 1개. 에이전트 워크스페이스 = 허용 폴더(NAS 공유를 호스트에 SMB/NFS 마운트). **화이트리스트 = 워크스페이스에 넣은 폴더, 안 넣으면 차단.** 블랙리스트는 채택하지 않는다.
- 파일 서버 용도 에이전트는 `readonly` 프로필 + `tools.fs.workspaceOnly: true` + 셸 exec 계열 도구 끔(G 단계).
- NAS 권한과의 자동 동기화는 없다. NAS 로컬 사용자 20명은 IX-Auth CSV 벌크로 1회 투입하고 부서(공유 폴더) 그룹에 수동 배치한다.
- 호스트는 별도 x86 미니PC·VM 을 권장하고 NAS 직접 설치는 PoC 로 한정한다(2코어 Celeron, RAM 10GB).
- 사내망 배치는 `gateway.bind: lan` + `publicOrigin: http://<NAS IP>:18800` 으로 도메인 없이 동작. 평문 HTTP 는 세션 탈취 위험이라 IP SAN 사설 인증서 리버스 프록시를 권장.

단계에 추가되는 항목:

| 단계 | 추가                                                                                                                                                                                                                                 |
| ---- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| F    | 사용자 관리 화면에 **CSV 가져오기**(IX-Auth `POST /admin/users/bulk`, 상한 500행) + 부서 일괄 배치. NAS 사용자 목록 CSV 예시                                                                                                         |
| G    | compose 에 NAS 공유 마운트 예시(`volumes: - /mnt/nas/rnd:/mnt/nas/rnd:ro`)와 부서 에이전트 워크스페이스 지정 예시. **SMB/NFS 마운트 워크스페이스 라이브 검증**(workspaceOnly·권한·성능) 1회. 사람별 에이전트 생성·부서 바인딩 절차서 |
| 배포 | DEPLOY.md 에 "사내망 IP:포트 프로파일"(bind lan, publicOrigin http, `__Host-` 쿠키 폴백 동작 명시, IP SAN 인증서 프록시 예시)                                                                                                        |
| I    | 인덱스 대상은 마운트한 NAS 폴더. 변환 산출물은 NAS 가 아니라 호스트 로컬 디스크에 둔다(NAS 쓰기 금지 원칙)                                                                                                                           |

### J 단계 - 관리자 화면 마감·결함 수정 (2026-09-08 추가)

브랜치 `chris/delivery-j`. 사용자 피드백(2026-09-08 오전) 반영.

1. **관리자 WS 권한 결함**: ix-auth 로그인 연결에서 `applyConnectionScopeCap` 이 브라우저 선언 `x-openclaw-scopes` 교집합으로 잘라 시스템 관리자도 `operator.admin` 을 못 받는다(시크릿 모드·계정 선택 등 비활성). ix-auth 세션은 역할 정의 스코프를 상한으로 그대로 쓰도록 수정, 테스트.
2. 사용자 표·감사 표 **full width**(포크 규칙).
3. **IX-Auth 콘솔 SSO**: 시스템 관리자가 `/admin/identity/` 로 들어가면 재로그인 없이 진입. 게이트웨이[<sup>1</sup>](#g1)가 IX-Auth 신원 교환으로 같은 사람의 콘솔 토큰[<sup>2</sup>](#g2)을 발급해 심는다. admin 이하는 기존대로 링크 미노출·중계 차단.
4. **부서·에이전트·접근 폴더 관리 화면** `/settings/departments`: 부서(그룹 `dept-*`) 목록·생성·이름·구성원, 부서별 에이전트 바인딩, 에이전트별 허용 폴더(마운트된 NAS 루트 아래 선택, 화이트리스트)·읽기 전용 여부·색인 폴더. 저장 시 게이트웨이 config 패치(`agents.entries`, `memory.search.extraPaths`) + `department_agents` 바인딩. superadmin 만.
5. 테스트 시드 정리 명령·문서: 깨진 세션(`Unassigned user own note` 등)·테스트 계정 삭제, 납품 전 초기화 절차(DEPLOY.md).
6. 감사 화면 다듬기: 역할 한국어 라벨, 설정 사이드바 항목 노출. 사용자 표의 부서를 이름으로.
7. 컨테이너 안 CLI 제약: `audit users`·`knowledge`·`cron add` 중 RPC 불필요한 것은 SQLite/로컬 직접, 필요한 것은 문서에 호스트 대안.

### K 단계 - J 잔여 마감 (2026-09-08 추가)

브랜치 `chris/delivery-k`. J 단계가 남긴 항목과, 감독의 18800 실측(J 이미지, admin 계정)에서 나온 결함 3건.

1. **부서 삭제** `/settings/departments`: 시스템 관리자 전용, 빈 부서만. 신원 서버 그룹과 포크 투영을 함께 치우고 풀려난 에이전트의 워크스페이스·색인 폴더를 회수한다. 절차는 [AUTH-DEPARTMENTS.md](AUTH-DEPARTMENTS.md) 9.3-1.
2. **부서 이름 변경**: 벤더에 그룹 이름 변경 API 가 없어 게이트웨이 표시 이름만 바꾸는 것을 유지하고, 그 이름을 읽는 화면을 통일한다(감사 표만 코드를 그대로 찍고 있었다).
3. **콘솔 로그아웃 404**: 콘솔의 로그아웃이 막아 둔 자기 로그인 폼으로 이어지던 것을 "게이트웨이로 돌아가기" 로 바꾼다. 세션이 유지되므로 재로그인 없이 다시 들어온다. [AUTH-IXAUTH.md](AUTH-IXAUTH.md) 7.2.
4. **사용자 표 다듬기**: 역할 미부여 계정의 빈칸을 "미지정" 으로, 역할 필터에 중재자 추가, 주소·부서 칸 폭 상한과 말줄임, 빈 목록 문구를 행 전체 블록으로, 좁은 폭 여백.
5. **xlsx 날짜 서식**: 색인 변환에서 날짜 셀이 일련번호로 남던 것을 셀 서식을 읽어 `YYYY-MM-DD`(시각이 있으면 `YYYY-MM-DD HH:mm`)로. [KNOWLEDGE.md](KNOWLEDGE.md) 5절.
6. **시작 번들 예산 기준선**: `pnpm check` 가 이 검사를 돌리지 않는다는 것을 확인하고 기준선을 건드리지 않는다(아래 5절 K 행).
7. **권한 칩 라벨**: `tools.permissionMode` 가 설정된 에이전트의 칩이 실제보다 넓게(전체 액세스) 표시되던 것을 좁은 쪽으로 고친다.
8. **사용자 표 역할 빈칸**: 4번에 포함.
9. **감사 표 세션 열 잘림**: 폭 상한·말줄임·`title` 전체 값.

## 4. 사용자 결정 (2026-09-08 전부 결정)

기다리던 항목이 모두 답을 받았다. 기술 작업이 남은 것은 없고, 값을 넣는 시점만 배포로 미뤄져 있다.

| #   | 항목                    | 결정                                                                                                                                         | 반영 위치                                                                  |
| --- | ----------------------- | -------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------- |
| 1   | IX-Auth 라이선스        | 자사 제품이라 동봉·재배포에 제약 없음 (2026-09-08)                                                                                           | DEPLOY.md 8절                                                              |
| 2   | 도메인·TLS              | `jinbio.botops.cloud` + Cloudflare Tunnel. PoC·데모·초기 운영용이고 정식 납품 시 진바이오테크 자체 도메인으로 이전 (2026-09-07)              | DEPLOY.md 0절 7번·7절·11.7, compose `--profile tunnel`                     |
| 3   | SMTP                    | 배포 후 세팅. 그때까지 WEBHOOK 유지 (2026-09-08)                                                                                             | DEPLOY.md 3.8, 4.2 2번                                                     |
| 4   | 약관 문안               | 납품 시 고객이 제공 (2026-09-08)                                                                                                             | DEPLOY.md 4.2 4번                                                          |
| 5   | 부서 에이전트 API 키    | 배포 후 세팅. OpenAI·Anthropic 중 한 쪽만 채우면 된다 (2026-09-08)                                                                           | DEPLOY.md 3.4 (라), `ixauth.env.example`                                   |
| 6   | 감사 보존기간·질문 본문 | 90일, 질문 본문 미기록. 현재 기본값과 같아 코드 변경 없음 (2026-09-08)                                                                       | DEPLOY.md 3.7                                                              |
| 7   | 시드 계정 8개           | 로컬 스택에는 유지, 납품 스택을 세울 때 정리해 관리자 계정만 남긴다 (2026-09-08)                                                             | DEPLOY.md 4.1 (다)                                                         |
| 8   | 제품명                  | 임시값 "Chris Agent" 유지. 납품 직전 `src/brand.ts` 한 줄로 바꾼다 (2026-09-08)                                                              | FORK.md 5-1, DEPLOY.md 4.2 3번                                             |
| 9   | 시작 번들 예산          | 기준선을 실측값으로 갱신 (2026-09-08)                                                                                                        | `config/control-ui-startup-budget-baseline.json`, 5절 L 행                 |
| 10  | 에이전트 구성           | **`main` 하나만 납품한다.** 부서(`dept-rnd`·`dept-qa`)와 사용자·테스트 계정은 유지하고 부서 에이전트 `rnd-bot`·`qa-bot` 은 뺀다 (2026-09-08) | DEPLOY.md 3.3·3.3-1, AUTH-DEPARTMENTS.md 0절, 정본 템플릿 `agents.entries` |
| 11  | 모델                    | OpenAI 기본 `openai/gpt-5.6-luna`, Claude 예비 `anthropic/claude-sonnet-5`. 채팅 모델 선택 목록은 이 둘만 (2026-09-08)                       | DEPLOY.md 3.4, 정본 템플릿 `agents.defaults.model`·`modelPolicy.allow`     |
| 12  | CLI 카탈로그 노출       | 세션 사이드바의 "새 세션 - Claude Code"·"새 세션 - Codex" 를 숨긴다. 런타임은 그대로 둔다 (2026-09-08)                                       | DEPLOY.md 3.4, 정본 템플릿 `plugins.entries.*.config.sessionCatalog`       |

배포 시점에 값만 넣으면 되는 네 가지(모델 API 키·SMTP·제품명·약관)는 [DEPLOY.md](DEPLOY.md) 4.2 "납품 배포 체크리스트" 에 한자리에 모아 두었다.

계약 협의로 남는 것(기술 작업 없음): 부서 간 기밀을 계약 문구에 넣을지 여부(넣으면 부서별 게이트웨이 셀 분리가 필요하고 약 3주가 더 든다), 카카오톡 채널 필요 여부(제안서 문구는 "카카오톡이나 텔레그램처럼" 이라 텔레그램으로 충족한다고 본다).

## 5. 진행 기록

| 단계 | 상태 | 브랜치             | 머지 커밋                                                                                          | 비고                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| ---- | ---- | ------------------ | -------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| D    | 완료 | `chris/delivery-d` | `45ef6d3eec7` (ff)                                                                                 | 2026-09-07 22:15. 실측: 로그아웃 메뉴·WS 4001 종료, pptx·docx·xlsx 변환 미리보기(한글 정상), `cron.list`·`talk.catalog` 오류 0건. 증적 chris-server `analysis/2026-09-07-openclaw-auth/delivery-d-*.png`. 알아 둘 것: WS 종료는 프로필 단위(다른 기기 연결도 순간 끊김 후 재연결), 종료 사유 문자열은 업스트림 `operator role changed` 그대로, 시작 번들 예산 선재 초과 +164B                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| E    | 완료 | `chris/delivery-e` | (ff)                                                                                               | 2026-09-07 23:20. 실측: 초대 역할 선택지 직원·임원·관리자(+시스템 관리자), 임원 선택 시 부서 전체 체크, 체크 해제 후 발급해도 서버가 dept-qa·dept-rnd 채움(DB 확인), 임원 계정이 R&D·QA 두 부서 타인 세션 열람 가능·쓰기는 "전송되지 않음", 계정 화면 역할 "임원"·사용자 관리 링크 없음. 증적 `delivery-e-*.png`. 같이 들어간 것: 납품 템플릿에 codex 런타임(`plugins.entries.codex.enabled`)과 기본 모델, ChatGPT 구독 device-code 로그인 절차(DEPLOY.md 3.4)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| F    | 완료 | `chris/delivery-f` | (ff)                                                                                               | 2026-09-08 01:40. 실측: `/settings/users` 목록(한국어 역할·부서 필터·10명), 역할 변경 즉시 반영(직원→임원, 활성 세션 3→0), 부서 저장(dept-qa+dept-rnd DB 반영), 비활성/재활성, 임원 계정의 `/auth/admin/users` 403. 증적 `delivery-f-01-users-page.png`. 알아 둘 것: 삭제는 소프트 삭제(비활성+세션 종료), 재설정 링크는 메일로만(메일 없으면 초대 재발송), 목록의 부서는 코드로 표시(이름 표시는 후속), 시작 번들 +435B(기준선 선재 초과, baseline 미조정), 관리자(admin)는 IX-Auth 콘솔 중계를 여전히 통과(콘솔 링크는 superadmin 만 노출)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| G    | 완료 | `chris/delivery-g` | (ff)                                                                                               | 2026-09-08 04:30. 들어간 것: `readonly` 도구 프로필, `tools.permissionMode` 기본값(세션이 넓히려면 admin), cron 만든 사람 투영, NAS 마운트 배선(`OPENCLAW_NAS_ROOT`, 샘플 `chris-local/nas-sample`), 에이전트별 `skipBootstrap`, doctor 검사 `codex/agent-workspace-boundary`, 문서(경로 A·사내망 IP:포트). 실측: rnd-bot 세션 권한 "읽기 전용"·워크스페이스 `/mnt/nas/rnd` 렌더 확인. **결함 확정**: ChatGPT 구독(codex 하네스) 경로에서는 부서 에이전트가 파일을 읽지 못하며(컨테이너에서 bwrap 사용자 네임스페이스 불가), 셸을 살리면 codex 읽기 루트가 `/` 라 `/mnt/nas/qa` 까지 읽혀 경계가 깨진다. 따라서 읽기를 열지 않았고 **부서 에이전트는 API 키 기반 네이티브 런타임이 필요**(사용자 결정 5번 추가). 근거 AUTH-DEPARTMENTS.md 13.9                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| H    | 완료 | `chris/delivery-h` | (ff)                                                                                               | 2026-09-08 07:10. 원장 `audit_user_activity`(kind 9종), 기록 지점(로그인·로그아웃·로그인 거절·질문·파일 읽기·전사 열람·파일 열람·관리자 행위·접근 거부), RPC `audit.userActivity.list`(superadmin 전부·admin 자기 부서), CSV `/auth/admin/audit/export.csv`, CLI `openclaw audit users`, UI `/settings/audit`, 보존 `logging.audit.userActivity.retentionDays`(기본 90)·`maxRows`. 실측: 로그인·로그인 거절 행이 화면·SQLite 에 기록, 종류 필터 9종·CSV 링크. 정본 `AUTH-AUDIT.md`. 후속 다듬기: 표의 역할이 코드(superadmin)로 표시, 설정 사이드바 "개인정보 보호 & 보안" 절에 감사 항목 링크 미노출(주소 직접 진입은 됨), 컨테이너 안 CLI 는 ix-auth 모드에서 게이트웨이 RPC 인가가 없어 실행 불가(기존 제약, `audit users` 는 SQLite 직접 읽기로 바꾸면 해결). 결정 대기: `promptText` 기본 off 유지 여부, 보존기간                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| I    | 완료 | `chris/delivery-i` | (ff)                                                                                               | 2026-09-08 09:20. `openclaw knowledge sync`(pdf·docx·xlsx·pptx·txt(cp949)·md → frontmatter 사이드카, 증분·삭제 정리·dry-run·JSON), 색인 폴더 마운트 `OPENCLAW_KNOWLEDGE_ROOT` 와 부서 에이전트 `extraPaths` 배선, `memory.search.provider: "none"`(FTS 전용, 임베딩 API 호출 제거), 정본 `KNOWLEDGE.md`. 컨테이너 실측: rnd 8건 변환, 재색인 10파일, "에탄올 재고"·pptx·xlsx·cp949 질의 히트, qa-bot·main 은 No matches(경계 유지). 주기 실행은 호스트 systemd 타이머(컨테이너 안 `cron add` 는 ix-auth RPC 제약). 한계: xlsx 날짜 일련번호, 사이드카 이름 `.pdf.md`, hwp 제외                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| J    | 완료 | `chris/delivery-j` | `983afae343e` (ff)                                                                                 | 2026-09-08 10:20. 7항목 전부 구현. 1) ix-auth 로그인이 매핑된 역할을 `user_profiles.role` 에 투영하고, 연결 스코프를 역할 정의에서 그대로 펼친다(요청은 상한이 아니다). 실측: 선언 스코프 0개로 붙은 시스템 관리자에게 operator.admin 포함 6개 부여. 2) 사용자·감사 표 full width(stacked 설정 행 안에서 표 상자가 접히던 것). 3) 관리 콘솔 SSO - 프록시가 그 사람의 세션 access token 을 Authorization 으로 주입하고 콘솔 문서에만 부트스트랩을 심는다. 콘솔 자체 로그인 404, 통과 조건 superadmin 전용(ADMIN 도 `ixauth:*:*` 라 이중 방어가 아니었다). 4) `/settings/departments`(부서 목록·생성·이름·구성원, 에이전트 바인딩 RPC 3종, NAS 루트 아래 폴더 선택·읽기 전용·색인 폴더). 부서 삭제는 넣지 않았다. 5) `chris-local/reset-seed.sh` + DEPLOY.md 4.1. 깨진 세션 `agent:main:dashboard:600bbf52...` 실제 삭제 확인. 계정은 지우지 않았다. 6) 감사 표 역할 한국어, 사이드바 감사 항목(비관리자 그룹 목록에서 빠져 있었다), 사용자 표 부서 이름. 7) `openclaw audit users` 가 게이트웨이가 답하지 못할 때만 SQLite 직접 읽기로 폴백. 같이 고친 것: `upsertDepartment` 가 슬러그만 아는 호출에 이름을 덮어쓰던 결함, 부서 폴더 루트를 컨테이너 밖 변수에서 읽던 것                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| K    | 완료 | `chris/delivery-k` | `fdc3b837012` (ff)                                                                                 | 2026-09-08. J 잔여 6항목 + 감독 실측 결함 3건. 1) 부서 삭제: `DELETE /auth/admin/departments`, 시스템 관리자 전용, 빈 부서만(인원수는 신원 서버 그룹 구성원 목록이 1순위, 실패 시 로컬 투영). 그룹 먼저 지우고 투영 나중이라 중간 실패는 화면이 이미 아는 고아 상태로 남는다. 신원 서버가 더는 나열하지 않는 고아 행은 그룹 호출 없이 지운다. 풀려난 에이전트의 workspace·읽기 전용·색인 폴더는 화면이 `config.patch` 로 비운다(게이트웨이 HTTP 경로는 설정을 못 쓴다). 감사 kind `admin_action`, action `department-delete`. 2) 이름 변경: 벤더에 그룹 이름 변경 API 가 없어 표시 이름만 바꾸는 것을 유지. 감사 표만 부서 코드를 그대로 찍고 있어 이름 표시로 통일(사용자 표·부서 화면·초대·사용자 상세는 이미 이름이었다). 3) 콘솔 로그아웃 404: 원인은 콘솔의 `logout()` 이 막아 둔 자기 로그인 폼(`/admin-ui/api/login` = 404)으로 이어진 것. 프록시가 콘솔 문서 끝에 스크립트를 붙여 `window.logout` 을 덮어쓰고 버튼을 "게이트웨이로 돌아가기" 로 바꿔 `/settings/users` 로 보낸다. 세션이 유지되므로 재로그인 없이 다시 들어온다. 덮어쓰기는 반드시 콘솔 스크립트 뒤에 있어야 한다(앞에 두면 함수 선언이 이긴다). 4)+8) 사용자 표: 역할 미부여 계정의 빈칸 -> "미지정", 역할 필터에 중재자 추가(빠져 있었다), 주소·부서 칸 폭 상한·말줄임(`title` 에 전체 값), 마지막 로그인 보조색, 빈 목록·불러오는 중을 행 전체 블록으로, 720px 이하 여백 축소(표는 계속 가로 스크롤). 5) xlsx 날짜: `xl/styles.xml` 의 `cellXfs` 를 읽어 날짜 서식이면 `YYYY-MM-DD`(시각 있으면 `+ HH:mm`). 내장 14~~22·27~~36·45~~47·50~~58 + 사용자 서식(리터럴·대괄호 제거 후 y·d / h·s 판정, m 은 단독 판정 없음). 1900-02-29 보정, 분 단위 반올림. 1904 기준 통합 문서는 미지원(틀리면 4년 어긋난다). 문서 미리보기도 같이 고쳐진다. 6) 시작 번들 예산: **`pnpm check` 는 이 검사를 아예 돌리지 않는다**(lint 레인은 oxlint·stylelint·i18n verify 뿐). 강제는 `pnpm ui:check-performance`(CI 레인, 프로덕션 빌드 후)에서만 일어나고, 기준선 자체가 위반이 되는 조건은 `startupJsGzipBytes` 가 고정 상한 358400 B 를 넘을 때다. 지금 기준선은 350377 B 로 상한 아래이고, 실측치 허용선은 min(기준선, 상한) + 512 + 64 = 350953 B 다. **`pnpm check` 기준으로는 실패가 아니므로 기준선을 건드리지 않았다.** 다만 이 브랜치를 프로덕션 빌드해 `pnpm ui:check-performance` 를 직접 돌려 보면 실측 352964 B 로 허용선을 2011 B 넘겨 실패한다(기준선 대비 +2587 B). 기준선 갱신은 4096 B 래칫 안이라 가능하고 갱신해도 고정 상한 358400 B 아래이므로, 갱신할지 UI 를 줄일지는 감독 결정 사항으로 남긴다. 7) 권한 칩: `listAgentsForGateway` 가 exec 정책만 보고 라벨을 만들어 `tools.permissionMode: read-only` 인 에이전트가 "전체 액세스" 로 떴다. exec 라벨과 설정된 모드 중 좁은 쪽을 내보내도록 수정(서버만 고치면 UI 는 따라온다). 9) 감사 표가 컨테이너보다 넓었다. 1280px 뷰포트 실측에서 표 상자 788px 에 표 978px(시각 149·사람 138·역할 92·부서 44·종류 81·요약 227·세션 248) 이라 세션 열이 상자 밖에 있었고, 세션 칸만 좁혀도 표가 줄지 않았다. 자동 표 레이아웃에서는 선언한 폭이 권고라서다. 1121px 이상에서 `table-layout: fixed` + 열 비율로 바꾸고, 말줄임이 나오도록 모든 칸에 클리핑을 주고 잘릴 수 있는 칸에 `title` 을 붙였다. 비율은 자연 폭이 아니라 각 열이 담아야 하는 가장 긴 문구로 잡는다(역할 "시스템 관리자", 종류 "관리자 행위", 시각 전체 지역 시각). 확정 **19/12/13/9/12/17/18**(788px 기준 약 150·95·102·71·95·134·142). 줄어드는 몫은 줄바꿈하는 요약과 전체를 읽을 일이 없는 세션 키가 낸다. 자연 폭으로 잡은 첫 두 시도는 각각 고정 어휘와 시각을 몇 px 씩 잘랐다. 요약은 계속 줄바꿈하므로 가장 좁은 몫으로도 읽힌다. 그 아래 폭은 기존 가로 스크롤 유지. 사용자 표도 같은 방식(6열 18/20/12/16/14/20)으로 맞췄고 실측에서 표 788px = 컨테이너 788px, 잘리는 셀 0개(142·158·95·126·110·158)로 통과했다. **감독 실측(2026-09-08, 18800)**: 항목 1(부서 삭제 거부·성공·감사 행)·3(콘솔 버튼 "게이트웨이로 돌아가기" 복귀)·4+8(역할 "미지정")·5(xlsx 날짜 2026-09-01)·7(권한 칩 "기본값 (읽기 전용)") 통과. 항목 9 는 세 번의 실측을 거쳐 통과(가로 스크롤 제거 -> 고정 어휘 잘림 제거 -> 시각 잘림 제거). J 이미지에서 이미 정상이던 것: 사용자 표 full width·부서 이름, 부서 화면(부서 2·에이전트 바인딩·폴더), 감사 표 역할 한국어, 콘솔 SSO 재로그인 없이 진입.                                                                                                                                                                                                                                                                                                                                                                                                     |
| L    | 완료 | `chris/delivery-l` | `299496394bc` (ff)                                                                                 | 2026-09-08. 사용자 결정 9건 반영 + 자동 배포 잔재 재발 방지. 1) **Cloudflare Tunnel 프로파일**: compose 에 `cloudflared` 를 `profiles: ["tunnel"]` 로 추가(이미지 `cloudflare/cloudflared:2026.8.3`, 실제 최신 릴리스 태그). 토큰은 argv 가 아니라 `TUNNEL_TOKEN` 환경변수로 넘긴다. argv 로 두면 `docker inspect`·프로세스 목록에 값이 보이고, 프로파일 안의 `${VAR:?}` 가드는 프로파일 필터보다 먼저 해석돼 프로파일을 안 켠 `compose config`·`up -d` 까지 실패시킨다(실제로 그렇게 실패하는 것을 보고 고쳤다). public hostname 의 대상은 호스트 `127.0.0.1:18800` 이 아니라 compose 네트워크의 `http://gateway:18789` 다. 2) **Cloudflare 뒤 동작을 코드로 확인**: `isSecureGatewayBrowserContext`(`src/gateway/cookie-header.ts:135-150`)가 보안 컨텍스트로 인정하는 것은 TLS 종단·루프백·신뢰 프록시가 보낸 `x-forwarded-proto: https` 셋뿐이고, 마지막 것은 `isTrustedProxyAddress`(`src/gateway/net.ts:196`)가 `gateway.trustedProxies` 에서 주소를 찾을 때만 읽힌다. cloudflared 는 compose 네트워크의 컨테이너라 게이트웨이가 보는 출발 주소가 172.x 이므로, 그 목록이 비어 있으면 로그인은 되지만 쿠키가 평문 배포 모양(`__Host-` 없음, `Secure` 없음)으로 남는다. 그래서 `OPENCLAW_TRUSTED_PROXIES` 를 신설해 `start-gateway.sh` 가 쉼표 목록을 JSON 배열로 렌더링하고 템플릿의 `gateway.trustedProxies` 에 넣는다. 비우면 `[]` 이고 `isTrustedProxyAddress` 가 빈 목록을 목록 없음과 같게 다루므로 기존 동작이 그대로다(그래서 평문 HTTP 프로파일은 손해가 없다). `allowedOrigins` 는 문자열 완전 일치라 `https://jinbio.botops.cloud` 를 그대로 통과시키고, `issuer`·`audience` 는 고정 리터럴이라 주소와 무관하다. 그 밖에 필요한 게이트웨이 설정은 없었다. 3) **모델 API 키 전달 결함**: `ANTHROPIC_API_KEY`·`OPENAI_API_KEY` 가 게이트웨이가 아니라 `ix-auth` 서비스 환경에 붙어 있었다(주석은 게이트웨이가 읽는다고 적혀 있었다). 게이트웨이로 옮겼다. 4) **자동 배포**: `auto-deploy.sh` 가 pull 전에 빌드가 다시 쓰는 추적 파일을 되돌린다. 목록은 각 확장 `package.json` 의 `openclaw.assetScripts.buildOutputs` 에서 읽어 만든다(지금은 workboard 매니페스트 1개 + canvas 번들 3개). 실제로 수정된 것만 되돌리고 이름을 로그에 남기며, 그 밖의 더러움은 예전처럼 FAIL 이고 `git status --short` 가 로그에 함께 찍힌다. 5) **시작 번들 예산**: 기준선 350377 -> **352955 B**(실측, Node v24.20.0). +2578 B 로 4096 B 래칫 안, 고정 상한 358400 B 아래. 갱신 후 `pnpm ui:check-performance` 통과(허용선 353531 B). **실측**: `pnpm check` exit 0·실패 0(WSL). compose `config` 는 프로파일 없이 서비스 3개, `--profile tunnel` 로 4개. 프로파일 없이 `up -d` 후 4컨테이너 healthy, `http://127.0.0.1:18800/` 200, 렌더된 설정의 `trustedProxies` 는 `[]`(무변경 확인). 로그인 화면 스크린샷 `delivery-l-01-login.png`. 템플릿 렌더링은 값 있음·없음 두 경우 모두 유효 JSON. `auto-deploy.sh --force` 를 plugin.json 을 일부러 수정해 둔 상태로 돌려 `reverted build outputs before pull: extensions/workboard/openclaw.plugin.json` 로그와 `OK: deployed 299496394bc0 ... HTTP 200` 을 확인했다. **잔여**: 터널 자체는 만들지 않았다(사용자 Cloudflare 계정 자산이라 범위 밖). `https://jinbio.botops.cloud` 실접속·`__Host-` 쿠키 복귀는 터널을 만든 뒤 확인해야 한다. FAIL 경로의 `git status --short` 출력은 코드로만 확인했고 라이브로 재현하지 않았다.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| M    | 완료 | `chris/delivery-m` | `0bf73af4e12` (ff)                                                                                 | 2026-09-08. CI/CD 와 진바이오 NAS 배치. 1) **CI**: 새 워크플로 `.github/workflows/chris-deliver-images.yml` 이 `chris/main` 푸시마다 게이트웨이·ix-auth 이미지를 굽고 `ghcr.io/necromman/openclaw-gateway`·`openclaw-ix-auth` 에 민다(태그 `chris-main` + `sha-<짧은 커밋>`, 레지스트리 캐시, 러너 디스크 확보 단계, 패키지 public). 빌드 인자는 compose 의 `gateway.build.args` 와 같아 브라우저·한글 폰트·LibreOffice 가 납품 이미지에 들어간다. 실측 16분 9초(게이트웨이 15분 4초, ix-auth 1분 6초), 이미지 4.71 GB·274 MB. 2) **NAS 배치 파일**: `chris-local/nas/docker-compose.nas.yml`(빌드 없이 ghcr 이미지, 컨테이너별 `mem_limit`, mailpit 없음)과 `deploy.sh`(ghcr 태그 다이제스트 비교 -> pull -> `up -d` -> 헬스 대기 -> `image prune`, `mkdir` 잠금, `--force`·`--status`). 3) **NAS 설치**: `/volume1/docker/openclaw/` 에 compose·`deploy.sh`·`ixauth-gateway-config/`·`nas-sample/`·`knowledge-index/`·`ixauth.env`(0600, 값은 새로 생성). 컨테이너 4개 healthy/running, 메모리 gateway 504 MiB/3 GiB·ix-auth 378 MiB/768 MiB·db 132 MiB/512 MiB·cloudflared 18 MiB/128 MiB, 호스트 여유 7,541 MB. 기존 컨테이너 7개는 건드리지 않았다. 4) **CD**: `/etc/crontab` 에 5분 주기 root 항목 1줄 + `synoservice --restart crond`. `deploy.log` 에 5분 간격 "변경 없음", `--force` 는 "OK: 배포 완료(헬스까지 0초)". 5) **실측**: `https://jinbio.botops.cloud/` HTTP 200, 로그인 화면·관리자 로그인 성공, 쿠키 `__Host-openclaw-session-csrf`(Secure 확인), 사용자 관리에 관리자 1명뿐(시드 계정 없음), cloudflared `Registered tunnel connection` 4줄. 스크린샷 chris-server `analysis/2026-09-07-openclaw-auth/delivery-m-01-jinbio-login.png`·`delivery-m-02-jinbio-admin.png`. 6) **겪은 함정 2개**: docker-compose 1.28.5 는 `depends_on.condition: service_healthy` 를 오래 기다리지 않고 `up` 을 실패시켜(게이트웨이 첫 기동 1분 30초) cloudflared 만 짧은 형식으로 바꿨다. Windows Git Bash `openssl` 이 CRLF 를 내는 탓에 env 값 안에 CR 이 남으면 compose 가 그 자리에서 값을 잘라 관리자 비밀번호가 짧아진다. 둘 다 DEPLOY.md 13절에 적었다. 7) 잔여: 실제 공유 폴더 매핑(11.4), 모델 API 키(3.4 (라)), SMTP(3.8)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| N    | 완료 | `chris/delivery-n` | (ff)                                                                                               | 2026-09-08. 채팅 첨부 소유권·부서 경계·재참조. 1) **결함**: `/__openclaw__/assistant-media` 의 `sessionKey` 가 선택 파라미터라 빼고 부르면 세션 검사가 만들어지지 않았고, 인바운드 참조는 폴더 컨테인먼트를 항상 통과한다. 로그인만 돼 있으면 id 를 아는 누구나 남의 첨부를 받을 수 있었다. 2) **소유권**: 공용 DB 에 feature-local `inbound_media`(id PK·session_key·agent_id·profile_id·original_name·mime·size_bytes·created_at·deleted_at). 부서 열은 두지 않고 세션과 같은 방식으로 `department_agents` 에서 파생한다(AUTH-DEPARTMENTS 6-3 과 같은 근거). chat.send 의 첨부 영속화 지점에서 이미지·비이미지 모두 기록하고, 인라인 이미지의 원본명을 위해 `ChatImageContent.fileName` 을 추가했다. 3) **게이트**: `src/gateway/inbound-media-access.ts` 가 단일 판정. 비 ix-auth 통과 → 시스템 관리자 통과 → 행 없으면 404(레거시) → 소유자 통과 → 부서 검사 → 관리자 통과 → 세션 가시성(`sessions.others` 상한). 참조 문자열에서 id 를 뽑으므로 `media://` 형태와 저장소 절대경로 형태가 같은 게이트를 지나고, 티켓 경로도 reader 에 검증된 부서 사실을 실어 같은 판정을 받는다. 거부는 404 + 원장 `access_denied`. 4) **재참조 도구**: `media_list`·`media_read`(카탈로그 media 절, `readonly`·`coding`·`messaging` 프로필). 범위는 도구 인자가 아니라 세션 생성자 프로필 + 현재 에이전트의 부서 바인딩으로 서버가 정한다. `media_read` 는 문서 색인과 같은 `convertKnowledgeSource` 로 텍스트를 뽑고(20MB·60000자 상한), 이미지는 이미지 블록이다. 두 도구가 다 있을 때만 시스템 프롬프트에 순서 한 줄이 붙는다. `media_read` 는 원장 `tool_read` 로도 남는다(`media_list` 는 본문 열람이 아니라 제외). 5) **보존**: 세션 삭제는 `deleted_at` 표시만 남기고 파일은 두며, 납품 템플릿에 `attachments.ttlHours` 를 넣지 않는다. 실측(compose 18800): 첨부 전송 후 `inbound_media` 2행(원본명 `2026-1분기-연구개발-계획.pdf`·`favicon-32.png`), **새 세션**에서 `media_list` 2건·`media_read` 로 PDF 본문 요약, 시스템 관리자 curl 200(application/pdf, 53590B)·부서 없는 직원 404(9B), `sessionKey` 를 붙여도 404, 원장에 `access_denied{reason:department-boundary,surface:assistant-media,mediaId}`. 증적 chris-server `analysis/2026-09-07-openclaw-auth/delivery-n-1*.png`. 알아 둘 것: 이 표가 생기기 전 첨부(레거시)는 소유자가 없어 시스템 관리자만 열 수 있다                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| O    | 완료 | `chris/main` 직접  | `a1a6bcb8b13`                                                                                      | 2026-09-08. 납품 구성 확정 2건 + 선재 결함 2건. **이 단계 도중 작업 규칙이 바뀌었다**: 브랜치 없이 `chris/main` 에 직접 커밋하고, WSL 검증 왕복 대신 Actions 이미지 빌드와 운영 실측으로 판정한다(CLAUDE.md 4절, FORK.md 3절, HANDOFF.md 6절). 1) **에이전트 하나**: 정본 템플릿의 `agents.entries` 에서 `rnd-bot`·`qa-bot` 을 빼고 `main` 만 남겼다. 부서(`dept-rnd`·`dept-qa`)와 사용자·테스트 계정은 그대로 두고, NAS 상태 DB 의 `department_agents` 바인딩은 `openclaw agents department --clear` 로 지웠다. `start-gateway.sh` 의 자리표시자가 여덟에서 셋(오리진·가입 스위치·신뢰 프록시)으로 줄었다. compose 의 공유·색인 마운트는 남겼다. 되켜는 절차는 [DEPLOY.md](DEPLOY.md) 3.3-1 에 모았고, 11.4·12절은 그 절차의 일부라고 표시했다. 2) **모델**: 기본 `openai/gpt-5.6-luna`, 예비 `anthropic/claude-sonnet-5`(`model.fallbacks`, 복수형). 채팅 모델 선택 목록은 `agents.defaults.modelPolicy.allow` 로 이 둘만. 예비 모델은 allow 에 함께 적어야 사용자가 직접 고를 수 있다(`model-selection-shared.ts` 의 `addConfiguredRef` 주석). 3) **CLI 카탈로그 숨김**: `plugins.entries.{codex,anthropic}.config.sessionCatalog.enabled=false`. 카탈로그 provider 등록과 노드 명령만 건너뛰고 `registerAgentHarness` 는 그 조건문 바깥이라(`extensions/codex/index.ts`) main 의 구독 로그인이 타는 codex 런타임은 그대로다. 코드 수정 없이 설정으로 해결했다. 4) **결함 A(내부 메일 웹훅 403)**: 터널 뒤 배포가 `gateway.trustedProxies` 에 컨테이너 네트워크를 올리는데 신원 서버가 같은 네트워크에 있어, `/auth/mail-hook` 이 "전달 헤더 없는 프록시" 로 판정돼 403 이었다. 판정이 라우팅보다 먼저 도는 자리라 서비스 키는 읽히지도 않았다. 귀속 불가 분기에서 `runIxAuthServiceKeyRoute` 를 한 번 태운다. 그 계획은 mail-hook 하나만 claim 하므로 세션 라우트는 여전히 거절된다. 함께 cloudflared 에 compose 고정 주소(`172.16.240.10`, 대역 `172.16.240.0/24`)를 주고 신뢰 프록시를 그 한 주소로 좁혔다. 5) **결함 B(관리자 사용자 화면)**: 세션 프로브가 "관리자인가" 를 superadmin 에게만 오는 `adminConsoleUrl` 로 판정해, 서버가 200 을 내주는 `/settings/users`·`/settings/audit` 을 화면이 잠갔다. 역할(`user.gatewayRole`)을 `superadmin`·`admin` 과 맞추도록 고쳤다(`isIxAuthAdminRole`). 부서 화면은 그대로 superadmin 전용이다. 6) **함께**: `agents department --clear` 가 설정에서 사라진 에이전트의 바인딩도 지우도록 고치고 남은 행을 목록에 보여 준다(`--set` 은 그대로 거부). `server-http.ts` 가 700줄 상한을 넘어 인증 네임스페이스 진입 인자를 한 곳에 모았다(698줄). **실측** - 로컬 compose 18800: 신뢰 프록시를 게이트웨이 자신의 주소로 좁혀 재현했을 때 mail-hook 204(키 틀리면 401, 같은 출처의 `/auth/me` 는 여전히 403), 모델 선택 2개, 사이드바에 Claude Code·Codex 없음, `admin2@deploy.local`(role=admin, `adminConsoleUrl` 없음)로 `/settings/users` 11명 목록, 부서 화면은 여전히 거절. **운영 `https://jinbio.botops.cloud`**: 배포 전 같은 호출이 403 `proxy_attribution_required` 였고 배포 후 204. `admin.test@deploy.local` 로 `/settings/users` 7명, 초대 1건이 내부 웹훅으로 링크까지 노출(ix-auth 로그 `메일 웹훅 전달: to=o-webhook-test@deploy.local kind=INVITE`), 모델 선택 2개(GPT-5.6-Luna 기본·Claude Sonnet 5), Sonnet 3초 "O-NAS-SONNET-OK"·Luna 9초 "O-NAS-LUNA-OK", `models status --agent main` 이 `Allowed models (2)` 와 `openai via codex status=usable`, `agents department --json` 이 에이전트 `main` 하나·`staleBindings` 비어 있음. 증적 chris-server `analysis/2026-09-07-openclaw-auth/delivery-o-0*.png` 9장                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    |
| P    | 완료 | `chris/main` 직접  | `cfc14187a34`                                                                                      | 2026-09-09. 설정 메뉴 역할 분리 + 관리자 권한 경계 재조정 + 시스템 관리자 계정 보호. 1) **설정 메뉴 세 단계**: ix-auth 모드에서 `/settings` 왼쪽 메뉴가 역할로 갈린다. 직원·중재자·임원은 프로필·화면 설정·알림·음성 대화·정보만 본다. 관리자는 거기에 사용자·부서·감사 기록·승인을 더한다. 나머지(Gateway·채널·기기·에이전트·모델·메모리·고급·디버그·로그·업데이트)는 `operator.admin` 이 있어야 쓸 수 있어 시스템 관리자만 본다. 판정은 `ui/src/app-navigation.ts` 의 `isSettingsNavigationRouteVisible` 하나이고 술어는 `ix-auth-admin-access.ts` 에 모았다(`isIxAuthRestrictedAccount`·`isIxAuthAdminOnlyAccount`). 항목별 표는 [AUTH-IXAUTH.md](AUTH-IXAUTH.md) 5-1. 2) **주소 직접 접근**: 메뉴에서 감추는 것으로 끝내지 않고 같은 판정이 라우트 아웃렛을 "권한 없음" 안내로 바꾼다(`ui/src/app/settings-route-access.ts`). 아웃렛을 그리지 않으므로 그 화면의 로더가 게이트웨이에 아무것도 묻지 않는다. 하위 페이지는 상위 항목의 판정을 따른다. 토큰 모드는 그대로다. 3) **부서 관리를 관리자에게**: `/auth/admin/departments` 의 생성·이름 변경·삭제에서 `rejectNonSuperAdmin` 을 없앴다. 경계를 지키는 것은 그대로 남는다 - 누구도 자기 부서를 못 고치고(`rejectSelfTarget`), 관리자는 시스템 관리자 행을 못 건드리고, 사람이 남은 부서는 지워지지 않는다. 4) **시스템 관리자 계정 보호**: 부서 변경에 `rejectSuperAdminTarget` 이 빠져 있어 관리자가 시스템 관리자의 부서를 바꿀 수 있었다(운영에서 200 으로 재현). 2단계 인증 초기화·세션 강제 종료에도 같은 판정을 넣었다. 앞의 것은 그 계정의 보호를 약하게 만들고 뒤의 것은 되돌릴 수 있는 사람을 밀어내는 데 쓸 수 있다. 화면은 그 행의 조작을 흐리게 하고 이유를 한 줄로 적는다. 표는 AUTH-IXAUTH 5-2. 5) **`necromman@gmail.com` 승격**: 운영에서 `admin@deploy.local` 로 SUPERADMIN 으로 올렸다(승격은 세션을 끊으므로 다시 로그인해야 한다). `infra/local/jinbio-deploy.md` 표 갱신. 6) **O 단계 회귀 아님**: 관리자에게 사용자 메뉴가 사라졌다는 보고는 배포 전 번들이 남아 있던 것이다. 같은 시점 운영에서 `admin.test@deploy.local`(role=admin) 이 `/auth/me` 에 `gatewayRole: "admin"`, `/auth/admin/users` 200, 메뉴에 사용자·감사 기록이 모두 있었다. 7) **실측**(운영 `https://jinbio.botops.cloud`, 이미지 `sha-e72a69a4ff6e` = `sha256:b96a9664`, NAS `deploy.log` `2026-09-09 00:41:24 KST OK: 배포 완료 (헬스까지 70초)`): `rnd.member@deploy.local`(직원) 왼쪽 메뉴가 프로필·화면 설정·알림·음성 대화·정보 5개뿐이고 `/settings/users` 직접 접근이 "관리자만 이 설정 화면을 열 수 있습니다" 로 막힌다. `admin.test@deploy.local`(관리자) 메뉴가 프로필·화면 설정·알림·사용자·부서·음성 대화·감사 기록·승인·정보 9개이고 `/settings/advanced` 직접 접근은 같은 안내로 막힌다. 관리자가 시스템 관리자 행(`admin@deploy.local`)을 열면 역할 select·부서 체크박스·저장·잠금 해제·2단계 인증 초기화·세션 강제 종료·비활성화가 전부 비활성이고 안내문이 하나 붙는다. API 로도 부서 변경·2단계 인증 초기화·세션 강제 종료가 403 `forbidden`(배포 전 같은 호출은 부서 변경이 200 이었다), 같은 세션으로 일반 직원의 부서 변경은 200. 관리자가 부서 `ptest` 생성 200·삭제 200, 직원의 같은 호출은 403. 스크린샷 chris-server `analysis/2026-09-07-openclaw-auth/delivery-p-0{1,2,3}-*.png`. 8) 알려진 예외: 중재자는 역할 정의상 `operator.approvals` 를 가지지만 승인 화면은 관리자 전용으로 두었다(채팅 사이드바 승인 알림은 그대로). 승인 화면을 중재자에게 열지는 사용자 결정                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| Q    | 완료 | `chris/main` 직접  | `b5760968c05` · `d98c98df2e8` · `64dc42c1f2b`                                                      | 2026-09-08. 사용자 지시 4건. 1) **에이전트 이름 "공용"·온보딩 생략**: `agents.entries.main` 의 표시 이름을 "공용"(설명 "회사 공용 AI 비서", `identity.name` 도 같이)으로 바꿨다. 첫 대화에서 비서가 "원하시는 이름을 정해주세요" 라고 되묻던 원인은 워크스페이스 `BOOTSTRAP.md` 였다(첫 절차가 "무엇이라고 부를지 물어라"). `start-gateway.sh` 가 기동마다 `/config/workspace-seed/` 의 `IDENTITY.md`·`SOUL.md`(한국어, 이름 고정, 회사 비서 역할)를 워크스페이스에 렌더하고 `BOOTSTRAP.md` 를 지운다. 게이트웨이는 정체성 파일이 템플릿과 다르면 그 워크스페이스를 "설정 끝남" 으로 기록하므로(`workspaceProfileLooksConfigured`, `src/agents/workspace.ts`) 다시 시드되지 않는다. 대상은 `workspace` 와 `workspace-main` 둘 다. 2) **사이드바 이름 잘림**: 업스트림은 말줄임 대신 마스크 그라디언트로 끝을 흐리게 해서 "Shared Assist" 처럼 잘린 것인지 원래 이름인지 구분되지 않았고 캐럿과 붙어 보였다. `ui/src/styles/fork-style.css` 8절에서 마스크를 끄고 `text-overflow: ellipsis` 를 켜며 이름과 캐럿 사이에 4px 를 준다(RTL 셀렉터도 같이 무력화해야 특정성에서 이긴다). 3) **세션 가시성 "본인만"**: `tools.sessions.visibility` 를 `self` 로 내리고, **직원·중재자의 `sessions.others` 를 `none` 으로 낮췄다**. 사람 축의 세션 목록을 정하는 것은 가시성 값이 아니라 역할 상한이기 때문이다(`sessions-board-inventory.ts` 의 행 술어가 `prepareSessionSharing` + 부서 게이트만 곱하고, `createProfileSessionEntryFilter` 는 `sessionCap !== "none"` 이면 남의 비-draft 세션을 전부 통과시킨다). 값만 바꿨다면 직원이 전 세션을 보게 되어 정반대 결과였다. 부서 게이트는 이 값에 매달려 있어 함께 꺼지지만, 지금 에이전트는 미바인딩 `main` 하나라 게이트가 켜져 있어도 부서가 있는 사람 전원에게 `open` 이었다. 첨부 소유권은 `gateway.auth.mode` 로만 켜지므로 그대로다. 근거와 되켜기는 [AUTH-DEPARTMENTS.md](AUTH-DEPARTMENTS.md) 0·3-1절. 4) **"홈" 개인화**: 새 파일 `src/gateway/home-session-key.ts` 의 `resolveCallerMainKey` 가 ix-auth 모드에서 접속자 프로필 id 의 sha256 앞 16자를 붙인 main 키를 만든다(`main-u<16진수>`). 그 값을 핸드셰이크 스냅숏(`server/health-state.ts`)과 `agents.list`(`server-methods/agents.ts`) 두 곳에서만 바꾼다. 브라우저는 이 둘 중 하나로 홈 세션 키를 만들고 URL 은 그대로 `/chat/main` 이라, 사람마다 다른 대화가 같은 주소로 열린다. 호스트 쪽(크론·채널·CLI·샌드박스 판정)은 설정값을 그대로 써서 건드리지 않았다. 기존 공용 홈 `agent:main:main` 은 남고, 역할 상한이 허락하는 사람(시스템 관리자·관리자·임원)만 볼 수 있다. **코드 63줄 + 호출 2곳**, 최소안(홈 링크를 새 세션으로 바꾸기)은 쓰지 않았다. 5) **덤으로 찾은 결함 - 템플릿 설정이 반영되지 않았다**: 게이트웨이가 기동 로그에 `Config auto-restored from backup ... (missing-meta-vs-last-good)` 를 남기고 방금 렌더한 `openclaw.json` 을 last-good 백업으로 되돌린다. 마지막으로 받아들인 설정에 최상위 `meta` 가 있었는데 새 설정에 없으면 손상으로 보기 때문이고(`src/config/io.observe-suspicious.ts`), 게이트웨이는 마이그레이션을 한 번 기록하면 `meta` 를 스스로 쓴다. 진바이오 NAS 는 Q 템플릿을 밀어 넣은 뒤에도 이름 "직원"·가시성 `department` 인 옛 설정으로 돌아가 있었다. 템플릿에 `meta.migrations.modelPolicyAllowlist` 를 넣어 해결했다. 6) **배포**: NAS 는 `/config` 를 이미지가 아니라 호스트 폴더(`/volume1/docker/openclaw/ixauth-gateway-config/`)로 마운트하므로 저장소 템플릿만 고쳐서는 반영되지 않는다. 네 파일(`openclaw.json`·`start-gateway.sh`·`workspace-seed/*.md`)을 NAS 사본에도 복사했다(백업 `*.bak-q-20260908`). 7) **실측**: 이미지 빌드 [34244196167](https://github.com/necromman/openclaw/actions/runs/34244196167) 성공, NAS `deploy.log` 에 `2026-09-09 00:41:24 KST  OK: 배포 완료 gateway sha256:b96a9664a11e... (헬스까지 70초)`. `https://jinbio.botops.cloud` 에서 (가) 사이드바 이름 "공용", 긴 이름을 넣어 보면 `text-overflow: ellipsis` 로 "Shared Ass…" 처럼 말줄임되고 캐럿과 4px 떨어진다(계산 스타일 `mask-image: none`·`text-overflow: ellipsis`). (나) `rnd.member` 로 연 새 세션의 첫 질문에 이름을 되묻지 않고 "저는 진바이오테크 임직원을 돕는 회사 공용 AI 비서 '공용'입니다" 로 한국어 응답(13초). (다) `rnd.member` 사이드바에는 자기 세션 1개만, `admin.test` 에는 여섯 사람의 세션이 전부 보인다. (라) 두 계정의 홈 세션 키가 다르다: `rnd.member` 는 `main-u…4ad6`, `admin.test` 는 `main-u…d04a`, 주소는 둘 다 `/chat/main`. 옛 공용 홈의 온보딩 대화는 관리자 목록에 남아 있다. 배포 후 상태 볼륨의 `workspace`·`workspace-main` 에 `BOOTSTRAP.md` 가 없고 기동 로그에 `auto-restored` 줄이 0건이다. 증적 chris-server `analysis/2026-09-07-openclaw-auth/delivery-q-01..04-*.png`. |
| R    | 완료 | `chris/main` 직접  | `657a5e6bb33` -> `679f70a65e2` -> `d78ab7765ca`                                                    | 2026-09-09. NAS 폴더 접근권한 0~~1단계. 설계 정본 [NAS-FOLDER-ACL.md](NAS-FOLDER-ACL.md), 구현 결과는 그 문서 12절. 1) **0단계(NAS 실물)**: NAS 에 전용 그룹 `openclaw-ro`(gid 65540)·`openclaw-rw`(65541) 를 신설하고 공유 9개(`00_공용폴더`~~`06_박정현`·`디자인PC`·`이영준한의원`) ACL 에 추가만 했다. 기존 그룹·사용자 항목은 손대지 않았다. 게이트웨이 컨테이너에 `group_add: 65540` 을 주고 공유 9개를 `/mnt/nas/<공유>` 로 읽기 전용 바인드 마운트. 컨테이너 안에서 9개 전부 읽기 OK·쓰기 거부 실측. 상속이 끊긴 `보안폴더` 7개는 NAS ACL 이 계속 막는다(그것이 상한이다). 2) **표·판정**: `folder_access_rules`(경로·주체종류·주체 id·권한·상속·작성자·시각), 경로 정규화는 NFC 통일 + 상위 탈출 거부 + `#recycle`·`@eaDir`·`@tmp` 제외. 판정은 깊은 경로 우선 -> 개인 > 부서 > 역할 -> 같은 종류는 더 강한 제한. 규칙 없으면 숨김. 3) **RPC 다섯**: `folders.tree.list`(지연 확장·미리보기)·`folders.rules.list`·`folders.rules.set`·`folders.rules.clear`·`folders.subjects.list`. 거부는 숨김과 없음을 구분하지 않는 한 문장으로 통일. 4) **화면**: `/settings/folders`, 관리자 이상. 왼쪽 트리 + 오른쪽 부서·역할·개인 탭, 실제 적용 미리보기, 하위 일괄 적용. 5) **감사**: `admin_action`/`folder-rule-set`·`folder-rule-clear`, `access_denied`/`folder-tree`. 6) **운영 실측 결함 2건 수정**: 규칙 메서드 스코프를 `operator.read` 로 내렸고(관리자는 이 배포에서 `operator.admin` 을 받지 않는다, `6b03c6a7d64`), 미리보기가 숨김 폴더를 그대로 보여 주던 것을 고쳤다(`c6597af275a`). 7) **운영 실측 통과**: 관리자 트리 11개, 규칙 저장·상속·깊은 경로 우선, 미리보기에서 숨김 확인, 직원은 메뉴 없음 + 주소 직접 입력 차단, 감사 원장에 `folder-rule-set` 2건. 스크린샷 6장. 8) **남은 것**: 세션 파일·미리보기·색인 게이팅(2단계), 에이전트 파일 읽기와 쓰기(3단계)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| S    | 완료 | `chris/main` 직접  | `97b37672eb4` · `7698c39cf6a` · `95292cd3666`                                                      | 2026-09-09. 한글 문서(hwp·hwpx) 변환. 1) **수단 선택**: 이미지의 LibreOffice 7.4 는 한글 필터가 HWP 3.0 전용이라 실제 공유 파일 17개 중 **0개** 변환(종료코드 0 인데 산출물 없음), pyhwp 는 표 칸을 버리고 AGPL·유지보수 중단이라 둘 다 기각. **의존성 0 · 이미지 증가 0 의 내장 리더**를 새로 만들었다(`src/gateway/document-cfb.ts` CFB 리더, `document-extract-hangul.ts` hwp5·hwpx 본문 추출). 2) **색인**: `knowledge sync` 기본 `--include` 에 hwp·hwpx 추가, converter 값 `hwp-text`. 암호·배포용·HWP 3.0 은 `encrypted`·`distribution`·`legacy-format` 으로 사유가 따로 잡힌다. 3) **첨부·미리보기**: `media_read` 가 같은 리더를 쓰고, 파일 패널은 "미리보기 예정" 안내 대신 텍스트 HTML 을 보여 준다. 첨부 accept 목록과 hwp MIME 5종 매핑 추가. 4) **실측**: NAS 공유 7곳 2013~2025년 문서 18개 중 내용 있는 17개 전부 성공, 최대 59ms. 정본 [FILE-PREVIEW.md](FILE-PREVIEW.md) 9절. 5) **운영 실측**(2026-09-09 10:50 배포): 채팅에 hwp 를 첨부하니 본문이 추출돼 문서 종류를 답했고(`delivery-s-01-hwp-summary.png`), 컨테이너에서 `knowledge sync` 가 `created hwp-text` 로 사이드카를 만들었다. 6) **rhwp 재실측**(사용자 지시): Rust·MIT 단일 바이너리 `rhwp` 를 다시 재봤더니 표를 마크다운 표로(병합 셀 포함) 내주어 내장 리더의 최대 약점을 메운다. 다만 v0.8.3 부터는 `GLIBC_2.39` 를 요구해 bookworm 에서 실행이 안 되므로 **v0.8.2 에 고정**하고, `export-markdown` 이 일부 문서에서 본문을 통째로 잃는 결함(표본 68개 중 4개)이 있어 **rhwp 1순위 + 내장 리더 폴백 + 내용 글자 수 60% 하한** 으로 붙였다. 이미지 증가 +14.9 MB, 새 npm 의존성 0. 근거·비교표는 FILE-PREVIEW.md 9-5                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| U    | 완료 | `chris/main` 직접  | `0f0037c9279` -> `5cee99e1bcc` -> `36a7f6bd719`                                                    | 2026-09-09. 관리자 모델 관리 + 설정 보존. 1) **설정 보존(선행 조건)**: `start-gateway.sh` 가 매 기동 템플릿을 상태 볼륨에 덮어써서 화면에서 바꾼 값이 다음 재기동에 사라졌다. 후보 A(템플릿을 병합 기준으로만 쓰기)는 "관리자가 바꾼 값" 과 "템플릿이 새로 정한 값" 을 구분할 수 없어 템플릿의 모델 변경이 영영 도달하지 못한다. 그래서 후보 B(별도 파일)를 골랐다. 게이트웨이가 저장할 때 상태 볼륨의 `admin-overrides.json` 에 같은 값을 쓰고, 기동 스크립트가 렌더 뒤에 `merge-admin-overrides.mjs` 로 그 파일을 다시 얹는다. **관리자 편집 가능 영역**은 `agents.defaults` 와 `agents.entries.<이미 있는 id>` 의 네 이름뿐이다: `model`(기본·대체), `modelPolicy`(허용 목록), `utilityModel`, `imageModel`. 나머지(신원·부서·도구 정책·신뢰 프록시·워크스페이스 시드·최상위 `meta`)는 계속 템플릿이 이긴다. 납품이 약속한 불변 영역이고 브라우저에서 바꿀 수 있으면 운영이 아니라 기기 개조이기 때문이다. 파일이 없거나 JSON 이 아니거나 `version` 이 1 이 아니면 아무것도 하지 않고 렌더 결과를 그대로 둔다(사유는 stderr 한 줄). `meta` 는 병합 대상이 아니라 Q 단계 처리와 충돌하지 않는다. 되돌리려면 그 파일 하나를 지운다. 표는 [DEPLOY.md](DEPLOY.md) 3.4. 2) **허용 목록 정책**: 키는 `agents.defaults.modelPolicy.allow` 이고, 게이트웨이가 `provider/*` 항목을 이미 프로바이더 와일드카드로 읽는다(`parseConfiguredModelVisibilityEntries`, `src/agents/model-selection-shared.ts`). 그래서 고정 목록·프로바이더 전체 허용·혼합 세 상태는 별개의 모드가 아니라 하나의 목록이고, 채팅 모델 선택기와 새 세션 화면이 규칙 하나로 같은 답을 낸다. 코드에 새 정책 축을 만들지 않았다. 3) **화면**: 새 라우트 `/settings/model-catalog`(설정 > 모델(채팅)), **관리자 이상**. 업스트림 `model-providers` 는 `config.set`(`operator.admin`)으로 쓰기 때문에 열 수 없고, 어떤 모델이 답하는가는 사용자·부서와 같은 회사 운영 판단이라 그 판단만 하는 좁은 화면을 새로 두었다. 읽기는 관리자가 이미 가진 `operator.read` 의 `models.list`(view=all)·`models.authStatus`, 쓰기는 새 라우트 `PUT /auth/admin/models` 하나다. 그 라우트는 설정 문서가 아니라 이름 붙은 네 필드만 받고 모델 참조 정규식으로 검사한다(허용 목록 상한 100, 본문 4 KiB 한도에 맞춤). 기본·대체 모델이 허용 목록 밖이면 400 `model_not_allowed` 로 거절한다. 저장은 감사 원장에 `admin_action`/`model-policy` 로 남는다. 모델 인증(구독 로그인·토큰)을 넣는 것은 범위 밖이고 인증 상태만 보여 준다. 판정 표는 [AUTH-IXAUTH.md](AUTH-IXAUTH.md) 5-1, 사용법은 [MANUAL.md](MANUAL.md) 10절. 4) **실측 도중 고친 결함 2건**: 화면이 프로바이더마다 "자격증명 없음" 을 보이고 모델이 둘만 나왔다. `models.list` 는 카탈로그를 에이전트별로 만들고 에이전트를 지목할 수 없으면 호출을 거절하는데(`agents.ownership: "explicit"` 에서 agentId 를 빼면 그렇다), 두 호출을 `Promise.all` 로 묶어 둔 탓에 인증 상태까지 같이 비었다. `agents.list` 로 에이전트를 먼저 확인하고 `allSettled` 로 바꿨다. 인증 상태 어휘도 틀렸다(`warning`·`degraded` 는 없는 값). 답할 수 있는 셋 ok·expiring·static 을 인증됨으로 본다. 5) **실측**(운영 `https://jinbio.botops.cloud`, 이미지 빌드 [34302919747](https://github.com/necromman/openclaw/actions/runs/34302919747) 성공, NAS `deploy.log` `2026-09-09 11:44:31 KST OK: 배포 완료`): (가) `admin.test@deploy.local`(관리자) 메뉴에 "모델(채팅)" 이 보이고 열리며 프로바이더 8개·모델 88개를 읽는다. (나) 모델 하나 추가(`openai/gpt-5.6-sol`) 저장 200 `persisted:true` -> 컨테이너 `docker restart` -> 기동 로그 `admin overrides applied: agents.defaults.model, agents.defaults.modelPolicy` 와 함께 값 유지, `auto-restored` 줄 0건, `meta`·인증 모드·신뢰 프록시 그대로. (다) 채팅 모델 선택기에 그 변경이 반영돼 3개(GPT-5.6 Luna·gpt-5.6-sol·Claude Sonnet 5). (라) 전체 허용 토글을 openai·anthropic 둘 다 켜서 저장하면 허용 목록이 `["openai/*","anthropic/*"]` 이 되고 채팅 선택기에 13개가 뜬다. 재기동 후에도 유지. (마) `rnd.member@deploy.local`(직원)에게는 메뉴에 없고 주소를 직접 쳐도 "관리자만 이 설정 화면을 열 수 있습니다", `/auth/admin/models` 는 GET·PUT 모두 403 `forbidden`. 감사 기록에 `관리 행위 / model-policy` 두 줄. 실측 뒤 납품 기본값(두 모델 고정 목록)으로 되돌려 두었다. 증적 chris-server `analysis/2026-09-07-openclaw-auth/delivery-u-01..06-*.png` 7장. 6) **알려진 한계**: 프로바이더 전체 허용을 켜면 채팅 선택기가 첫 조회에서 전체 탐색을 기다린다(이 배포에서 약 15초, 그 뒤 캐시되어 즉시). 구독 로그인만 있는 프로바이더는 탐색 결과가 API 키가 있을 때보다 적다(openai 는 `view=all` 6개 중 선택기에 1~2개). 고정 목록에서는 이 지연이 없다                                                                       |
| V    | 완료 | `chris/main` 직접  | `da48894d22c` -> `d0ffdef53c1` -> `2dad03a3b16` -> `d0948323dff` -> `cfb183bb685` -> `092893d81fd` | 2026-09-09. 폴더 권한 2단계(적용 지점)와 한글 미리보기 표. 정본 [NAS-FOLDER-ACL.md](NAS-FOLDER-ACL.md) 13절. 1) **파일 목록·열기·미리보기**: 게이트 하나(`src/gateway/folder-access-guard.ts`)가 요청자 신원과 브라우징 루트를 받아 경로마다 판정한다. `sessions.files.list`·`get`·`set`, `agents.files.list`·`get`, `agents.workspace.list`·`get` 이 그 게이트를 지난다. 문서 미리보기는 `sessions.files.get` 의 읽기를 지나므로 따로 손대지 않아도 함께 닫힌다. 설계가 걱정한 "핸들러가 `client` 를 안 받는다" 는 구조분해에 한 단어를 더하는 것으로 끝났다(인자에 이미 있었다). **적용 범위를 좁게 잡은 것이 핵심**이다. 루트가 `/mnt/nas` 밖이거나, 검증된 신원이 없거나(호스트 셸), 시스템 관리자면 게이트를 만들지 않는다. 그러지 않으면 "규칙 없음 = 숨김" 이 에이전트 자기 워크스페이스까지 덮어 제품이 닫힌다. 거부는 없는 파일과 같은 문구이고 원장에 `access_denied`/`folder-read` 로 남는다. 검색은 숨김 폴더로 내려가지 않는다(건수로도 새지 않게). 2) **지식 색인**: 색인을 도는 것은 사람이 아니라 크론이고 사이드카는 공용 풀이라 사람별 필터가 없다. 그래서 판정을 폴더에 대해 낸다 - **누구 하나라도 볼 수 있는 폴더만 색인한다**. 걷기 자체를 잘라 파일 이름조차 읽지 않고 `folder-hidden` 을 폴더당 한 줄로 보고한다. 걷지 않은 폴더의 파일은 `known` 에 없으므로 기존 고아 정리가 사이드카를 지운다(회수 장치를 따로 만들지 않았다). 사이드카 frontmatter 로 판정하는 안은 판정 지점이 둘로 갈리므로 채택하지 않았다. **안전장치**: 규칙 표가 그 공유를 한 번도 언급하지 않았으면 기능이 켜지지 않은 것으로 보고 종전대로 색인한다(그러지 않으면 손대지도 않은 공유의 색인이 첫 규칙 하나에 통째로 비워진다). `--no-folder-rules` 로 한 번 끌 수 있다. 3) **고아 규칙**: RPC `folders.rules.orphans`·`orphansClear`. 고아는 두 종류(`missing-folder`·`missing-subject`)다. 자동 복구는 하지 않는다(이름 변경과 삭제를 구분할 수 없고, 짐작해 규칙을 옮기면 닫아 둔 폴더를 조용히 연다). 삭제는 두 번 묻고 서버가 목록을 다시 떠서 그때도 고아인 것만 지운다. 마운트가 없으면 전부 고아로 보이므로 거부한다. 화면은 `/settings/folders` 아래 절. 4) **한글 미리보기 표**: S 단계가 rhwp 로 얻은 마크다운을 색인에만 쓰고 있었다. 두 리더 선택 규칙을 `document-hangul-read.ts` 한 곳으로 모아 색인과 미리보기가 같은 답을 내게 하고, 변환기가 내는 좁은 마크다운(제목·파이프 표·목록·문단)만 HTML 로 그리는 렌더러를 새로 넣었다(**새 npm 의존성 0**, 저장소의 마크다운 라이브러리 둘은 개발 의존성이고 범용 렌더러는 원문 HTML 을 통과시킨다). 링크는 글자로 남긴다. 5) **실측 중 고친 결함 3건**: 포크 테스트가 R 단계부터 빨간 불이던 `normalizeFolderRulePath("/")` 가 `undefined` 를 내던 것(`cfb183bb685`), 표 칸의 리터럴 `<br>` 가 글자로 찍히던 것, 8열 양식이 좁은 패널에서 한 글자 폭으로 눌리던 것(`092893d81fd`). 6) **운영 실측**: 납품 구성은 에이전트가 `main` 하나이고 워크스페이스가 컨테이너 홈이라 게이트가 걸릴 표면이 없다. NAS 설정 템플릿에 임시 에이전트(`/mnt/nas/00_공용폴더`)를 붙여 확인하고 지웠다. 파일 패널 18개 -> 15개(`@eaDir`·`#recycle`·`#경영회의` 제거), `기록물` 이 목록에서 사라짐, 직접 열기 `session file not found`, 원장에 `folder_rule` 거부 한 줄, 색인에서 `folder-hidden` 폴더당 한 줄 + 사이드카 5개 회수, 고아 규칙 목록·두 번 묻는 삭제, 지출결의서 hwp 가 표로 렌더. 증적 chris-server `analysis/2026-09-07-openclaw-auth/delivery-v-01..06-*.png`. 7) **남은 것**: 에이전트가 도구로 읽는 파일과 쓰기(3단계). 색인 풀은 여전히 부서 경계가 아니다                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| W    | 완료 | `chris/main` 직접  | `60b799e941c` -> `66ba487d36c` -> `ce6bc25e7d9`                                                    | 2026-09-09. 운영 장애 두 건과 모델 화면 재설계. 1) **모델 인증 갱신 실패**: 사용자 화면에 `auth refresh request failed: code=-32603` 이 반복되고 질문마다 30초가 늦었다. 원인은 같은 ChatGPT 구독 로그인을 작업 PC 와 NAS 가 나눠 쓴 것이다. 구독 OAuth 는 갱신 때 refresh 토큰을 회전시키므로 PC 가 갱신할 때마다 NAS 사본이 죽는다(NAS 사본 `last_refresh` 2026-09-04, PC 2026-09-09). 갱신이 한 번도 성공하지 못해 영구 프로필이 만들어지지 않았고(`models auth list` 에 `openai:default` 없음), 파일만으로는 회복되지 않는다. 30초는 재시도가 아니라 취소되지 않는 토큰 fetch 타임아웃 하나이고(`TOKEN_REQUEST_TIMEOUT_MS`), 9초 래퍼는 race 라 하위 fetch 를 끊지 못한다. runtime-only 프로필의 갱신 실패는 평범한 `Error` 로 던져져 실패 쿨다운(`markAuthProfileFailure`)이 걸리지 않아 질문마다 같은 값을 다시 문다(업스트림 동작, 이 포크에서 고치지 않고 문서화만 했다). **조치는 사용자 확정대로 기본 모델을 `anthropic/claude-sonnet-5` 로 옮긴 것**이다. Anthropic 은 setup-token 장기 토큰이라 갱신 자체가 없다. Luna 는 목록에 남기고 대체 모델은 비웠다(깨진 OpenAI 를 대체로 두면 30초를 한 번 더 문다). 2) **WebSocket 폴백**: `Falling back from WebSockets to HTTPS transport` 는 게이트웨이도 Cloudflare 도 만들지 않는 문장이었다. 상태 볼륨의 Codex 런타임 바이너리가 OpenAI 로 가는 자기 스트림을 못 열었을 때 찍는 줄이고, 1) 과 한 몸이라 기본 모델을 옮기면 함께 사라진다. 터널 쪽 WS 오류(cloudflared 의 `connection refused`·`no such host`)는 게이트웨이 재생성 구간에만 몰려 있어 `originRequest` 는 손대지 않았다. 게이트웨이가 30초마다 `tick` 을 보내므로 Cloudflare 100초 유휴 한도 가설도 배제된다. 3) **모델 화면 재설계(사용자 지시)**: 프로바이더별 카탈로그 전체를 그리고 모델마다 스위치 하나. 저장값은 언제나 켜진 모델의 명시적 목록이고 `provider/*` 는 더 쓰지 않는다(와일드카드가 있으면 채팅 선택기 첫 조회가 15초 걸린다). 옛 저장본의 와일드카드는 열 때 펼쳐 보여 주고 저장하면 목록으로 바뀐다. 자격증명 없는 프로바이더는 켤 수 없고(켜면 선택기에 떴다가 즉시 실패한다) 이미 켜진 것은 끌 수 있다. 대체 모델은 끄면 자동으로 빠지고 기본 모델은 저장을 막는다. **선재 결함 2건 동반 수정**: 모델 ref 패턴이 슬래시 하나만 받아 `huggingface/deepseek-ai/DeepSeek-R1` 류를 거부했고(전체 켜기가 `invalid_body`), 허용 목록 상한 100 과 공용 본문 한도 4 KiB 로는 카탈로그 82개가 들어가지 않았다(상한 300, 이 라우트만 64 KiB). 한국어 문안은 TM(`ui/src/i18n/.i18n/ko.tm.jsonl`)이 영어 원문 해시로 매칭되므로 바뀐 키와 새 키의 한국어를 직접 넣었다. 4) **운영 실측(2026-09-09 15:07 배포 뒤)**: 프로바이더 8개·모델 82개가 스위치 91개로 보이고 인증 없는 프로바이더의 75개가 비활성. 모델 2개를 켜고 저장하니 직원 계정의 채팅 모델 선택기가 4개로 늘고, 1개를 다시 끄니 3개로 줄었다. 기본 모델은 Claude Sonnet 5 이고 응답 3초(전에는 30초 지연 뒤 폴백). 게이트웨이 재기동 뒤에도 `admin-overrides.json` 이 그대로 적용됐고(`admin overrides applied` 줄), 재기동 이후 로그에 `-32603`·`Falling back from WebSockets`·`model-fallback` 이 0건. 직원 계정은 메뉴에 항목이 없고 주소로 들어가면 '관리자만 이 설정 화면을 열 수 있습니다'. 증적 chris-server `analysis/2026-09-07-openclaw-auth/delivery-w-0*.png` 6장. **빌드 중 잡힌 것 2건**(커밋 `d7b3c49efe0`): 길이 상한 시험이 한 조각 160자 ref 를 기대했는데 한 조각은 128자가 상한이라 실패했고, 프로바이더 스위치의 이벤트 대상 단언에 SAFETY 주석이 없어 단언 래칫이 걸렸다. `chris-check` 의 `plugin boundaries` 실패는 선재(만료된 deprecation 창)로 그대로다. 5) **실측에서 드러난 것 하나 더**(커밋 `79c42bf6ca7` -> `44ee2f41b5e`): 배포 직후 로그인 리다이렉트로 이 화면에 도착하면 카탈로그가 비어 오고 그 빈 답이 페이지가 살아 있는 동안 최종값이 됐다(게이트웨이가 프로바이더 해석 전에도 `models.list` 에 답하고, 로그인 직후에는 화면이 읽는 신원도 아직 자리를 잡지 않는다). 2초 간격 여덟 번까지 다시 묻게 했다. 15:51 배포 뒤 재실측에서 로그인 직후 첫 열람에 카탈로그 82개·스위치 91개(비활성 75)가 그대로 나왔고, 재기동 세 번을 건너 기본 모델과 허용 목록 3개가 유지됐다.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      |
| X    | 진행 | `chris/main` 직접  | `adb2b4ea354` -> `e784cd9c0f4` -> `409f834b4b0` -> `43ab54c7cd3` -> `2e2a974c84b` -> `9801bc56100` | 2026-09-09. 폴더 트리 인덱싱과 문서 색인 운영. 정본 [NAS-FOLDER-ACL.md](NAS-FOLDER-ACL.md) 14절과 [KNOWLEDGE.md](KNOWLEDGE.md) 7.2~7.3·8-2. 1) **트리가 느렸던 진짜 이유**: 폴더 수가 아니라 한 단계를 그리는 비용이었다. 기존 목록 코드가 `readdir` 뒤 **이름 하나마다** `realpath`+`stat` 를 불러, 파일 3,127개가 든 폴더 한 단계가 856ms 였다(실측). `readdir(withFileTypes)` 로 바꾸고 심링크에만 `realpath` 를 내니 5ms 다. 부서 화면의 워크스페이스 선택기도 같이 빨라진다. 2) **폴더 트리 저장본**: 상태 DB 에 `folder_tree_nodes`·`folder_tree_scans` 두 표를 더했다(R 단계 관례대로 기능 지역 추가 스키마). 폴더만 담고 파일은 담지 않는다. `자식을 읽은 시각` 을 따로 두어 "하위가 없는 폴더" 와 "아직 안 읽은 폴더" 를 구분한다. 화면은 저장본을 읽고 **저장본이 없는 가지는 종전대로 NAS 를 직접 읽는다**. 갱신은 (가) 매일 새벽 cron + (다) 관리자 새로고침(고른 가지만)을 골랐고, (나) 펼칠 때 뒤에서 갱신은 넣지 않았다(펼침마다 NAS 훑기가 따라붙어 결국 같은 부하가 된다). 겹치기 방지는 프로세스 플래그가 아니라 상태 DB 행이다(크론의 CLI 와 게이트웨이가 다른 프로세스다). 3) **명령·RPC·화면**: `openclaw folders scan                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            | status | clear`(RPC 를 쓰지 않아 ix-auth 배포의 컨테이너 안에서 돈다), `folders.tree.refresh`RPC 하나, 트리 위의 마지막 갱신 시각과 새로고침 단추. 훑기는 동시성 2, 보안폴더에서 멈추지 않고 넘어가며, 없어진 폴더의 행만 지우고 **규칙은 건드리지 않는다**. 4) **트리 실측**: 공유 11개 **19,136 폴더를 17.5초**(반복 10.7초), 못 읽은 폴더 7개(= 보안폴더). 2절의 37,356 은 호스트에서 root 로 센 값이고 컨테이너가 보는 것은 19,136 이다. 화면 체감은 펼치기 145ms -> 105ms(터널 왕복 100ms 가 바닥), 그리고 **파일만 많은 폴더가 잎으로 그려져 클릭 자체가 사라진다**. 5) **공유별 권한**(사용자 확정, 두 번째 지시가 정본): **관리자 전용은`01_이화정`하나**이고`00_공용폴더`·`02`~`06`·`디자인PC`·`이영준한의원`여덟 개는 직원·중재자·임원 읽기다.`00_공용폴더/#경영회의` 는 숨김으로 두었다(판단, 되돌리기 한 줄). **민감 공유에 "관리자 읽기" 를 주지 않은 것이 핵심**이다. 읽기를 주면 그 폴더가 색인 대상 자격을 얻고, 사이드카 풀에는 사람별 칸막이가 없어 인사·급여가 공용 비서의 검색으로 새어 나간다. 관리자는 폴더 화면 트리(숨김도 보인다)와 NAS 탐색기로 본다. 규칙 32행, 미리보기(직원)에서 여덟 공유만 남는 것을 실측. 6) **색인 운영**: compose 의 색인 마운트를 부모째(`/mnt/knowledge`)로 바꾸고 호스트 폴더 소유자를 uid 1000 으로 고쳤다(아니면 첫 파일에서 Permission denied). 공용 비서 `main`에`memory.search.extraPaths`를 물렸다(U 단계 admin-overrides 는 모델 네 항목만 병합해 충돌하지 않는다).`nas/nightly-index.sh`+`/etc/crontab`한 줄(03:20)로`folders scan`->`knowledge sync`->`memory index`를 돌린다. deploy.sh 와 같은 mkdir 잠금·로그 회전을 쓰고 잠금·로그는 따로 둔다. **업무 시간을 피하는 이유**는 2코어 NAS 가 낮에는 회사 파일 서버라서다. 7) **첫 색인 범위**: 규칙 기준으로`00_공용폴더` 6,298 파일(원본 6.22GB), 제외는 확장자 밖 32,222·숨김 폴더 1,523줄·20MiB 초과 194. 나머지 7개 공유까지 합치면 **28,211 파일·16.4GB**(`01_이화정` 400개는 제외). 사이드카는 원본의 약 1.6%(평균 16KB)라 8개 전부라도 300MB 를 넘지 않는다. 2.9TB 여유에 용량은 제약이 아니고 **제약은 시간**(0.5개/초로 16~20시간, 밤 4~5회)이다. 8) **첫 색인 실행 중 결함 2건**: (가) 파워포인트 잠금 파일(`~$이름.pptx`)이 훑기와 stat 사이에 사라져 ENOENT 가 실행 전체를 끝냈다(711개에서 멈춤). 제외 사유 `vanished`로 넘긴다. (나) 암호 걸린 PDF 가 예외를 던져 또 끝냈다(1,163개에서 멈춤). PDF 경로에서 받아`encrypted` 로 보고하고, 그 위에서 변환기 호출 전체를 감싸 어떤 변환기가 무엇을 던져도 그 파일만 failed 가 되게 했다. **둘 다 사람이 쓰는 공유에서는 평범한 파일이다.** 9) **NAS 자체 색인 조사**(사용자 질문): Universal Search(SynoFinder)가 켜져 있고 공유 8개를 본문까지 색인 중이다. 그래도 쓰지 않는다. 본문 필드 중앙값이 0~59자이고 한글 문서는 8%만 본문이 있으며(우리 사이드카는 평균 16KB), 접근 경로가 호스트 root CLI 아니면 2단계 인증이 걸린 DSM 계정뿐이고, 그 색인은 우리 폴더 규칙을 모른다(`01_이화정`도 본문까지 들어 있다). 근거·수치는 KNOWLEDGE.md 8-2. **설계는 바꾸지 않았다.** 10) **운영 실측**: 규칙 저장·미리보기(직원 시야 8공유), 트리 저장본 표시와 가지 새로고침, 공용 비서에게 NAS 문서를 물어 **출처 경로와 함께** 답이 오는 것까지 확인. 증적 chris-server`analysis/2026-09-07-openclaw-auth/delivery-x-01..04-*.png`. 11) **남은 것**: 첫 색인이 진행 중이다(이 보고 시점 1,163/6,298). 결함 2건을 고친 이미지가 배포되면 야간 크론이 이어서 끝낸다. 나머지 7개 공유는 한 번에 하나씩 `SHARES` 에 더한다. |
| Y    | 완료 | `chris/main` 직접  | `7fc86e32d1c`                                                                                      | 2026-09-09. **OpenAI 를 걷어내고 Claude 단독 구성으로 바꿨다**(사용자 지시: "GPT 는 키 없애고, Anthropic 토큰으로 Claude 를 사용할 수 있게. CLI 는 필요 없다"). 1) **지운 것**: 상태 볼륨의 `codex-cli/auth.json`, NAS 호스트의 `codex-auth.json`, compose 두 파일의 `CODEX_HOME`·`OPENAI_API_KEY` 전달, `ixauth.env`(NAS·예제)의 `OPENAI_API_KEY` 슬롯, 템플릿·관리자 저장본의 OpenAI 모델. 사본은 NAS `/volume1/docker/openclaw/openai-removed-y-20260909/`. 인증 저장소에는 지울 OpenAI 프로필이 애초에 없었다(W 단계 장애로 영구 저장된 적이 없어 `models auth list` 에 `anthropic:manual` 하나뿐이었다). 2) **모델은 Claude 3종**: `claude-sonnet-5`(기본)·`claude-opus-5`(상위)·`claude-haiku-4-5`(경량), 대체 모델은 비움. 카탈로그의 Anthropic 7종을 운영 setup-token 으로 하나씩 실제 호출해 정했다: sonnet-5·opus-5·haiku-4-5·opus-4-8·fable-5 는 HTTP 200, `claude-fable-5-1` 은 400 `claude_code_version_too_old`, `claude-mythos-5` 는 404 `not_found_error`. 뒤 둘은 켜면 고르는 즉시 실패하므로 뺐고, opus-4-8·fable-5 는 부를 수 있지만 위 셋과 쓰임이 겹쳐 선택기를 짧게 두려고 뺐다. **이름이 카탈로그에 있다는 것과 이 자격증명으로 부를 수 있다는 것은 다른 말이다.** 3) **codex 플러그인을 껐다**(`enabled=false`). O 단계가 카탈로그만 끄고 런타임을 살려 둔 유일한 이유가 구독 로그인이었는데 그것이 사라졌다. 끄기 전에 Claude 경로를 먼저 확인했다: `anthropic/claude-*` 는 내장 러너가 `POST https://api.anthropic.com/v1/messages`(`api=anthropic-messages`)로 직접 부르고, `registerAgentHarness` 를 부르는 플러그인은 저장소 전체에서 `codex`·`copilot` 둘뿐이라 하네스와 무관하다. `anthropic` 플러그인은 프로바이더를 주므로 끄지 않았다. 4) **OpenAI 를 감추는 코드는 넣지 않았다(판단). 넣을 필요가 없었다.** codex 를 끄자 OpenAI 프로바이더가 관리자 모델 카탈로그에서도 통째로 사라졌다(실측: 프로바이더 8개 -> 7개, `models list --all` 의 `openai/` 0건). `openai` 플러그인 자체는 그대로 실려 있다(기동 로그의 플러그인 12개에 포함). 확인한 것은 "codex 를 끄면 OpenAI 모델이 카탈로그에서 사라진다" 는 사실이고 되켜면 돌아온다는 것이며, 그 안쪽 기전까지 파지는 않았다. 자격증명 없이 남은 다른 프로바이더(`claude-cli`·`github-copilot` 등)는 "인증 없음" 표시와 잠긴 스위치로 그대로 둔다(`blocked = !authenticated && !allowAll`) - 감추면 관리자가 무엇을 켤 수 있었는지와 되켜는 길을 화면에서 볼 수 없다. 직원이 보는 선택기는 `modelPolicy.allow` 가 정한다. 5) **문서**: DEPLOY.md 3.4 를 Claude 정본으로 다시 쓰고 (사) Claude setup-token·(아) OpenAI 되살리기 두 절을 새로 넣었다. (다)·(마) 는 "지금은 쓰지 않는다" 로 표시만 하고 기록은 남겼다. MANUAL.md 10절, HANDOFF 0-1·7절, infra/local/jinbio-deploy.md. 6) **운영 실측(2026-09-09 16:39:57 KST 배포 뒤)**: 관리자 새 세션 `anthropic/claude-sonnet-5` 2초, 직원(rnd.member) 새 세션 `anthropic/claude-sonnet-5` 2초, 직원이 선택기에서 Claude Opus 5 로 바꿔 물으니 `anthropic/claude-opus-5` 4초. 모델 선택기는 관리자·직원 모두 Claude 3개뿐(OpenAI 없음). 새 세션 화면에 "Claude Code"·"Codex" 실행 항목 없음. 재기동 뒤 기동 로그에 `admin overrides applied: agents.defaults.model, agents.defaults.modelPolicy` 가 찍히고 기본 모델·허용 목록이 그대로 유지됐다. 재기동 이후 로그 393줄에 `-32603` 0건, `Falling back from WebSockets` 0건, `codex` 0건, `model-fallback` 은 실패가 0건이고 Opus 수동 선택이 남긴 정보성 한 줄(`configured fallbacks disabled by user model override`)뿐. 두 번째 배포(17:04:57)로 재기동이 한 번 더 있었고 같은 값이 그대로 유지됐다(관리자 새 세션 Sonnet 3초). 스크린샷 chris-server `analysis/2026-09-07-openclaw-auth/delivery-y-01..08-*.png` 8장. 7) **못 한 것**: 상태 볼륨의 codex 런타임 npm 패키지(`/home/node/.openclaw/npm/projects/`)는 지우지 않았다. 플러그인이 꺼져 실행되지 않고, 지우는 것은 되살릴 때 다시 받아야 하는 일만 만든다.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| Z    | 완료 | `chris/main` 직접  | `d2274eb3bf5` -> `580dd22db6c`                                                                     | 2026-09-09. 사용자 지적 2건("클로드의 모든 모델이 안 뜬다", "다 출력될 때가 있고 늦게 출력될 때가 있다"). 정본은 [DEPLOY.md](DEPLOY.md) 3.4 표·(자)·(차), 사용자 안내는 [MANUAL.md](MANUAL.md) 2·10·11절. 1) **채팅 모델이 3개에서 9개가 됐다.** 관리자 모델 화면이 보여 주는 Anthropic 항목을 하나도 빠뜨리지 않고 운영 setup-token 으로 실제 호출했다. sonnet-5·opus-5·haiku-4-5·fable-5·fable-5-1·opus-4-8·opus-4-7·opus-4-6·sonnet-4-6 아홉이 HTTP 200 이고 전부 켰다. 켠 아홉 개는 채팅 화면에서도 한 번씩 답을 받아 봤다(모델이 자기 이름을 그대로 답한다). 2) **fable-5-1 의 400 은 헤더 한 줄이었다.** 응답 본문이 `Claude Code 2.1.75 does not support this model; version 2.1.251 or newer is required` 이고, setup-token 경로에서 게이트웨이가 보내는 user-agent 가 `packages/ai` 의 상수 `ANTHROPIC_CLAUDE_CODE_VERSION = "2.1.75"` 로 굳어 있었다. 템플릿에 `models.providers.anthropic.headers["user-agent"] = "claude-cli/2.1.251"` 한 줄을 넣어 200 으로 바꿨다. 코드 수정 0줄이고 다른 여덟 모델의 응답은 그대로다. 3) **끄고 남긴 것**: `claude-mythos-5` 는 헤더를 올려도 404 `not_found_error` 라 이 자격증명에 없는 모델이다. `claude-opus-4-5-20251101`·`claude-opus-4-5`·`claude-sonnet-4-5-20250929` 는 관리자 화면에는 나오지만 러너가 `Unknown model` 로 거절한다(디스커버리 목록과 러너의 해석 목록이 다르다). `claude-haiku-4-5-20251001` 은 200 이지만 `claude-haiku-4-5` 와 같은 모델의 날짜 고정본이라 이름이 같은 줄이 둘 생겨서 껐다. 4) **지연의 원인은 모델도 터널도 아니다.** 진단 프로파일러(`diagnostics.flags: ["reply.profiler"]`)를 잠깐 켜서 잰 결과, 한 턴은 답 준비 2.2초 + 컨텍스트 조립까지 3.9초 + Anthropic 첫 응답 0.6~~3.2초이고, 나머지는 비서가 스스로 하는 일이다. 사내 문서를 찾지 않는 질문은 7초에 끝나고, `/mnt/knowledge` 색인을 뒤지는 질문은 도구 실행이 14.1초에 시작해 79.8초에 끝났다(출력 토큰 2.5천 개, 답에 "진바이오텍 취업규칙 제51조" 근거가 붙는다). 웹소켓 프레임은 측정 내내 계속 들어왔고 기동 이후 `Falling back from WebSockets` 0건, 측정 시각 18:00~~19:20 KST 에 색인 프로세스 0개·게이트웨이 CPU 0.3%다. 5) **"한꺼번에 나온다" 는 모델 성질이다.** 업스트림 전송 계층이 Sonnet 5·Opus 5·Fable 5·Fable 5.1 에서는 스트림을 끝까지 모아 두었다가 한 번에 내보낸다(`packages/ai/src/transports/anthropic-transport-stream.ts` 의 `usesClaudeStreamingRefusalContract` -> `createDeferredEventBuffer`, 플러시는 `message_stop` 뒤 한 번). 중간에 거절 판정이 바뀌면 이미 보여 준 글자가 틀린 것이 되기 때문이고 끄는 설정이 없다. 실측으로 Sonnet 5 는 글자 수가 0 에서 301 로 한 번에 뛰었고 Haiku 4.5 는 12.9초부터 0.3초 간격으로 늘었다. **그래서 고치는 대신 흘러나오는 모델을 열었다** - Haiku 4.5·Opus 4.8/4.7/4.6·Sonnet 4.6 다섯이 이번에 켜지면서 사용자가 고를 수 있게 됐고, MANUAL.md 10절 표에 어느 모델이 흘러나오는지 적었다. 6) **긴 세션이 새 세션보다 빠르다**(48.5k 세션 10.5초 대 새 세션 14~~34초). 재기동 직후 첫 질문이 84초로 가장 느리다. 7) **경고로 남긴 것**: 색인이 `00_공용폴더` 3,302 파일·5만 조각인데 에이전트 SQLite 가 359 MiB 이고 컨테이너 밖에서 여는 데만 15초다(FTS 질의 자체는 1밀리초). 나머지 공유 일곱 개(2만 8천 파일)를 더하기 전에 응답 시간을 다시 재야 한다. 기동 로그에 `memory pressure: level=warning ... rss=1000.2 MiB` 가 이미 한 번 찍혔다. 8) **못 한 것**: 스트리밍 버퍼링과 lane-1 회수 예산은 업스트림 상수라 손대지 않았다. 상태 볼륨에 `openclaw-agent.sqlite.reindex-lock.sqlite` 가 남아 세션 시작 동기화가 `Memory reindex lock is held` 로 건너뛰어진다(검색 결과에는 영향이 없어 그대로 두었다). 9) **가장 큰 조절 손잡이는 사고 수준이고 결정 대기로 남겼다.** 작성칸의 사고 수준이 지금 High 인데 이 값은 우리 설정이 아니라 모델 계약에서 온다(`defaultsClaudeAdaptiveThinking` 이 Sonnet 5·Opus 5 에서 참이라 비워 두면 적응형/High 다). 같은 세션·같은 형식의 질문을 사고 수준만 바꿔 재면 High 14.5~~33.8초, Minimal 6.5~6.9초다. `agents.defaults.thinkingDefault` 한 줄로 내릴 수 있지만 답의 깊이가 함께 얕아져 사용자 결정 사항으로 둔다. 사람마다 필요할 때 내리는 길은 이미 있고 MANUAL.md 10절에 적었다. 10) **실측 증적**: chris-server `analysis/2026-09-07-openclaw-auth/delivery-z-01..03-*.png` 3장과 `delivery-z-live-verification.log`.                                                                                                                                                                                                                                                                                                                                                                             |
| AA   | 완료 | `chris/main` 직접  | `96ab0a9bbc3` -> `54b71b22204`                                                                     | 2026-09-10. 사용자 지적: 채팅 화면에 들어가면 사이드바 목록이 "Main Session" 하나 밑으로 전부 접힌다. 원인은 홈 세션 키가 사람마다 `<main>-u<16진수 16자리>` 로 갈라진 뒤에도 화면이 "내 홈 키 하나" 만 홈으로 봤다는 것이다. 옛 공용 홈 `agent:main:main` 이 조상 조회로 루트에 끼어들면 그것을 부모로 가리키는 세션이 전부 자식이 됐다. 1) 서버가 `src/gateway/home-session-key.ts` 의 `isHomeSessionKey` 로 홈을 모양(설정된 main 단어 또는 사람별 접미사)으로 판정하고 세션 행에 `home: true` 표식을 싣는다(목록·상세·검색 공통, 프로토콜 `SessionRowSchema`). 2) `sessions.describe` 가 `sessions.list` 와 같은 공유 필터를 조건 없이 적용해, 목록에 없던 세션이 채팅에서만 열리던 차이를 없앴다. 3) 화면은 그 표식(없으면 같은 키 모양 판별)으로 조상 주입·메인 행 집합·보관/삭제 보호를 판단한다. 실측 필요: 운영 로그인 후 홈 라우트와 채팅 라우트의 목록이 같은 평면인지, 에이전트가 스폰한 하위 세션의 묶임은 그대로인지.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    |

| AB | 완료 | `chris/main` 직접 | `34600cd7809` -> `3b6956f6817` | 2026-09-11. [ENTERPRISE-ACCESS-PLAN.md](ENTERPRISE-ACCESS-PLAN.md) 1단계의 코드 항목. 상세는 그 문서 11절. 1) P0-1: CSV 가져오기가 IX-Auth 결과를 줄 번호가 아니라 배열 위치로 연결하고 이메일을 대조한 뒤 부서를 붙인다. 응답 줄 번호는 스프레드시트 기준, 행마다 부여·실패 부서 목록. 2) P0-3: 벤더 트리에 `GET /admin/groups/{id}/members` 구현(계약에는 있고 구현이 없어 405 였다, `ix-auth/MODULE.md` 6절). 부서 삭제는 GET 실패 시 사용자 목록 전체를 걸어 세고, 그것도 실패하면 503 으로 거부한다. 부서 목록 인원수는 신원 서버 목록 기준이고 `memberCountSource` 로 출처를 밝힌다. 3) P1-2: 부서 필터는 신원 서버 목록 전체(100건씩, 상한 50페이지)를 거른 뒤 페이지를 잘라 총계·페이지가 부서 기준이다. 4) P1-3: 부서 변경 응답에 `failedDepartments` 를 싣고 화면이 실패한 부서를 이름으로 알린다. 5) P1-4: 동작 뒤 재조회에서 알림을 지우지 않는다. 검증: Java `compileJava` 통과, 변경 파일 oxlint·tsc 통과, 게이트웨이 테스트 8건 추가·수정(실행은 Actions). 실측 필요: 운영에서 CSV 두 명 등록 후 각자 부서 확인, 품질 필터 다음 페이지·총계, 부서 삭제 시 미로그인 구성원 차단, 초대 재발송 알림 유지. 한국어 문구는 번역 메모리에 직접 넣었다. |

## 2026-09-11 추가 분석 기록: 기업용 계정·NAS 권한 관리

- 산출물: [ENTERPRISE-ACCESS-PLAN.md](ENTERPRISE-ACCESS-PLAN.md), 루트 AGENTS 인덱스와 HANDOFF 연결. 코드 구현이나 납품 완료 단계가 아닌 개선계획이다.
- 실측: NAS 로컬 계정 20개·그룹 9개, 마운트 11개 모두 읽기 전용, 대표 루트 조회 성공·보호 하위 EACCES. 실제 사이트 사용자 8명·부서 2개, 폴더 스냅샷 19,130개·규칙 32개. 부서 필터 후 다음 페이지가 비어도 `8명 중 25명` 표시, IX-Auth 그룹 구성원 GET 2건 모두 405. 상세 근거·한계는 계획 1.1절과 private chris-server 증적에 있다.
- 미실행: 계정/그룹/ACL 저장·CSV 초대·삭제·재색인·배포 및 직원별 검색/회수 시나리오. CSV 행 연결과 검색 인가 관련 소스 지적을 운영 재현 완료로 취급하지 않는다.
- 후속: 실직원과 NAS 계정 연결 검토, 자료별 기대 권한 합의, 등록/목록/구성원 계약 수정, 승인된 공용 자료 파일럿, 관리 UX 통합, NAS 읽기 전용 가져오기, 사용자별 검색 인가·철회 순서. 새 정책·설정·영속 계약은 구현 전 결정한다.

## 용어 설명

1. <a id="g1"></a>**게이트웨이** - 모든 요청이 먼저 닿는 앞단 서버. 이 제품에서는 화면·인증·에이전트 실행을 한꺼번에 맡는 본체를 가리킨다.
2. <a id="g2"></a>**토큰** - 신원을 증명하는 문자열. 가진 쪽은 그 신원으로 접속할 수 있으므로 비밀번호처럼 다룬다.

## 2026-09-13 AC: 회사 관리자 화면과 배포 안내

- 사용자 승인: 사용자·부서·폴더 관리 분석, 한글화·관리 편의 개선, 배포 중 안내 및 자동 복귀 구현과 운영 배포.
- 변경: 폴더 한국어/권한 범위 설명, 선택 경합·조회 실패/재조회, 하위 예외 초기화·권한 표시 갱신, 역할 명시 저장, 독립 nginx 안내. 상세 분석과 남은 기업 권한 작업은 ENTERPRISE-ACCESS-PLAN 12절.
- 재현: 운영 브라우저 응답 지연으로 선택 폴더와 규칙 불일치, RPC 실패로 편집 가능·정상 0건 오표시 확인. NAS 게이트웨이/신원/DB 정상, 가시성 self 확인.
- 상태: `4a29b556a0a` → `2344c7a31b5` 앱 개선과 NAS 배포 파일 반영 완료. Windows/WSL 검사·테스트는 실행하지 않았다. Actions의 통과 항목과 전체 린트 중단, 운영 실측은 아래에 구분해 기록한다.
- 추가 실측: 사용자 8번 응답을 보류하고 7번을 선택한 뒤 응답을 풀면 상세가 8번으로 되돌아갔다. 부서도 qa 응답을 지연한 뒤 rnd를 선택하면 qa 구성원 4명이 표시됐다. 최신 요청만 표시하고 조회 중 기존 편집 대상을 비우는 방식으로 사용자·부서·부서 폴더 선택을 수정했다. 조회 실패는 빈 목록과 구분하고 재조회 버튼을 제공한다.
- 배포 안내 실측: NAS의 읽기 전용 nginx 임시 경로와 오류 처리 재진입을 수정했다. 초기 게이트웨이 재기동 중 한국어 HTML 503, 모바일 가로 넘침 없음, 준비 완료 후 113.2초에 원래 경로·쿼리·해시로 자동 복귀했다. 최종 이미지 교체에서도 HTML 503·API GET/POST JSON 503·CSS/JS 200, 189.8초 후 같은 주소로 HTTP 200 자동 복귀를 확인했다. 독립 프록시와 터널 경로도 운영 반영 완료다.
- 추가 생산 코드 증가 이유: 현재 선택·요청 수명과 실패 상태를 관리하고 명시적인 저장 중 편집을 차단하는 경계를 추가했다. 중복 실패 판정과 장황한 주석을 제거했으며 DB·제품 설정·권한 정책 변경은 없다.
- 운영 추가 수정: 게이트웨이 `/readyz`는 200인데 Docker 헬스체크가 10초 제한을 연속 초과했다. 동일 범용 명령을 제한 없이 실행하면 exit 0·27초, 고정 포트 `/healthz` 직접 조회는 200·247ms였다. NAS compose의 점검만 직접 조회로 바꾸고 배포 상태 목록에 maintenance를 포함한다. 범용 런타임 점검 로더의 비용은 별도 후속 최적화 대상이다.
- Actions: `2344c7a31b5`의 [이미지 빌드 34729613956](https://github.com/necromman/openclaw/actions/runs/34729613956) 성공. [검사 34729613865](https://github.com/necromman/openclaw/actions/runs/34729613865)의 서버 테스트·UI 20파일 394건 통과. 포맷·코어/ UI 타입·i18n·dead export 검사 통과 후 전체 코어 린트가 약 172초에 exit 143으로 중단돼 전체 check는 실패다. 코드 위반 메시지는 없으며 종료 원인은 미확정이다. 기존 워크플로 주석에도 같은 종료 이력이 있어 별도 CI 범위·실행 비용 조사로 남긴다. 첫 개선 커밋 `4a29b556a0a`의 이미지와 양쪽 테스트는 성공, 진행 중이던 check는 다음 푸시로 취소됐다.
- 저장·레이아웃 실측: 기존 폴더 규칙을 같은 값으로 저장하고 정책과 32건 유지 확인. 비활성 시험 계정은 역할 선택만으로 PUT 0회, 저장 시 1회와 성공 알림 확인 후 원래 MEMBER로 복원했다. 데스크톱 1440px·모바일 390px에서 문서 폭은 각각 1440·390px. Secure 쿠키, 실제 WebSocket 연결, 익명 관리 API 403, 비신뢰 프록시의 위조 전달 헤더 403 확인.
- 생산 LOC: UI/클라이언트/스타일 +474/-227, 순증 247줄. 테스트 +77/-3, 순증 74줄. 번역 파일은 별도 +232/-139. 독립 배포 안내의 HTML·CSS·JS·nginx는 새 기능 272줄이며 배포 파일 변경은 별도다. 이전 응답·실패 상태의 소유 경계, 명시 저장, 한국어 접근 안내와 모바일 터치 영역, 게이트웨이 밖에서 살아 있는 안내 기능에 필요한 증가다.
- CD 후속: 두 번째 교체에서 구형 docker-compose의 Docker API 응답 대기가 60초에 끝나 `up` 실패로 기록됐지만, Docker가 새 컨테이너를 실제 시작한 사실은 확인했다. 제한 연장이나 반복 재기동은 하지 않는다. 현재 스크립트는 실행 컨테이너가 아니라 로컬 이미지 태그를 비교하므로, 배포 결과 기록·다음 주기 판정이 실행 이미지와 준비 상태를 기준으로 삼도록 개선해야 한다. 이번 완료 판정은 실제 컨테이너 이미지와 공개 준비 상태를 별도로 대조한다.
- 최종 실측(10:32~10:34 KST): 실행 이미지 ID와 Actions 산출 이미지 ID가 같고 manifest digest는 `sha256:fc617c62db043594c704e1cb01c56899775ca403c94cd6744e73ef832cb02f36`이다. 게이트웨이·신원·DB·maintenance healthy, cloudflared running. 사용자 상세는 마지막 선택을 유지하고, 부서 전환은 rnd 5명 모두 해당 부서임을 확인했다. 구성원 조회 실패 시 검색 입력 0개·재조회 버튼 1개, 재조회 후 qa 4명 복구. 부서 폴더 실패 시 저장 불가·재조회 가능, 재조회 후 복구. 역할 명시 저장·원복과 기존 규칙 저장도 최종 이미지에서 다시 확인했다.
- 증적: private chris-server `analysis/2026-09-13-admin-ux/`에 전후 비교 JSON, 화면, 실제 상태·배포 안내·헤더 거부 결과와 CI 실패 로그를 보관한다. 로그인을 복제한 일부 이전 검증 세션의 갱신 실패와 idle 만료는 새 로그인 성공과 별도로 기록했다. 회사 권한 후속은 ENTERPRISE-ACCESS-PLAN 12절, 운영 후속은 위 CI·CD·세션 갱신 조사다. 마지막 기록 커밋은 이미 운영 적용한 NAS 파일과 문서만 담고 이미지 재빌드는 생략한다.
- 추가 확인: 사용자 품질 부서 필터는 총 4명·표시 4명·다음 버튼 비활성·전원 품질 소속, 표 폭 942px로 정상이다. NAS compose·배포 스크립트·nginx 설정·자동 복귀 JS의 SHA-256이 작업 파일과 일치한다.

## 2026-09-13 AD: 사용자 상세 모달과 권한 진입점 통합

- 승인: 사용자 상세를 모달로 개선하고 사용자·부서·역할의 기존 권한을 사용자 화면에서 설정, 운영 배포까지 진행.
- 구현: 계정 / 역할·부서 / 폴더 권한 탭, 저장 중 닫기 차단, 변경 버리기 확인, 목록 포커스 복귀. 사용자 폴더 탭은 개인 예외·부서 공통·역할 공통 대상을 명시한다. 부서 목록에도 폴더 권한 모달 진입점을 추가한다.
- 소유 경계: 폴더 편집은 기존 folders-page와 RPC를 재사용한다. IX-Auth 계정 ID와 개인 ACL 프로필 ID를 혼용하지 않도록 관리자 POST가 신원 서버의 이메일을 확인한 뒤 로그인과 같은 ensureProfileForEmail로 정규 프로필을 준비한다. 첫 로그인 전에도 가능하며 새 스키마·제품 설정·권한 정책은 없다.
- 검증 준비: 기존 사용자 관리 테스트를 모달 구성에 맞게 조정하고, 개인 프로필 준비의 관리자·CSRF·대상 보호·멱등성·위조 본문 무시와 고정 대상 폴더 패널 회귀 검증을 추가했다. 한국어 19문구 추가, i18n 기준선 재생성. Windows/WSL 테스트·check는 실행하지 않는다. Actions와 배포 실측 결과는 후속 기록한다.
- 한계: 임의의 새 역할/보안 정책을 만드는 기능은 아니다. 공용 AI 검색 결과의 사용자별 인가는 별도 기업 권한 과제이며 화면에도 표시한다.
- 추가 재현: 운영 기존 전체 폴더 화면에서 권한 초안 1건 작성 후 같은 폴더 재선택만으로 0건이 됐다. 공유 선택 소유자에서 같은 폴더는 유지하고 다른 폴더 이동은 기존 확인 모달로 변경 버리기를 묻는다. 확인 중 대상 전환/해제는 취소한다. 역할 편집 대상은 기존 정규 역할 목록으로 고정해 사용자 역할 저장으로 편집 중 대상이 사라지지 않도록 했다. 전체 부서·역할 대상은 자유 설정 요구에 따른 의도이며 공통 적용 안내를 표시한다.
- 첫 Actions에서 UI 테스트 통과, assertion guard가 폴더 속성의 새 타입 단언 1건을 지적했다. Lit의 PropertyValues<this>로 직접 타입을 전달해 단언을 제거했다. 후속 커밋에서 다시 확인한다.
- 추가 실측/수정: 비활성 시험 계정에서 이름 미저장 입력 후 역할을 저장하자 이름 초안도 초기화됐다. 역할은 MEMBER로 복원했다. 이제 저장한 필드와 변경하지 않은 필드만 재조회 결과로 갱신하고 다른 초안을 보존한다. 부서 부분 실패 때는 요청한 선택을 남겨 재시도할 수 있다. 초안 병합과 닫기 판정을 한 모듈로 공유하고 회귀 3건을 추가했다.
- 최종 코드 `583a0813fda`: [이미지 빌드 34732170081](https://github.com/necromman/openclaw/actions/runs/34732170081) 성공, manifest `sha256:bc5a5779ce9c902828f9be632b6f75d6e444d9346b39a78a7e8dcc8166f3f582`, image ID `sha256:59f92a28a340fdb3425665316ca0ab50b9d624b79e08b5058e998179c9b4f26b`. [검사 34732170090](https://github.com/necromman/openclaw/actions/runs/34732170090)의 서버 테스트와 UI 20파일 400건 통과. 정적 검사와 운영 반영은 별도 완료 확인 중이다.
- 생산 LOC(8dabc523c8e 대비): +851/-171, 순증 680줄. 테스트 +168/-20, 순증 148줄. 번역은 별도 44줄, 문서는 별도다. 증가분은 사용자/부서 모달, 기존 폴더 편집기의 고정 대상 재사용, 정규 계정 연결, 초안 보존·확인과 반응형 스타일에 따른 신규 기능이다. 별도 ACL 저장 흐름이나 DB 스키마는 추가하지 않았다.
- 정적 검사 결과: `583a0813fda`의 코어 테스트 타입(684.5초), UI 타입, i18n, dead export 검사 통과. 배열 sort 대신 toSorted, 조건문 중괄호, 변수 shadowing 지적 4건으로 lint 실패했다. 해당 항목과 동일 패턴의 새 부서/폴더 코드도 수정해 후속 커밋에서 다시 확인한다.
- 운영 1차 실측(`583a0813fda`): HTML 안내 503 / API GET·POST JSON 503 / 안내 CSS·JS 200, 187.0초 후 원래 경로·쿼리·해시로 자동 복귀. 실행 image ID가 Actions 산출물과 일치했다.
- 모달 실측: 데스크톱 1440×900에서 x=180,y=67.5,1080×765; 모바일 390×844에서 화면 전체 390×844, 문서 폭 390으로 가로 넘침 없음. 기존 상세는 y=828에서 시작했다. 역할 선택 시 쓰기 0회, 저장 1회, 원래 MEMBER 복원. 이름 초안은 역할 저장 뒤에도 유지됐다. 개인/부서/역할 고정 대상, 같은 폴더 재선택과 다른 폴더 이동 취소 시 초안 유지, 대상 전환 변경 버리기, 모달 닫기 후 행 포커스 복귀, 부서 모달과 ESC 종료를 확인했다. 브라우저 pageerror 0건, 익명/CSRF 없는 POST 모두 403.
- 추가 요청 구현: 폴더 개인별 탭의 빈 검색어에서 전체 프로필을 기본 표시하고 긴 목록은 패널 내부에서 스크롤한다. 기존 운영은 대상 8명인데 0명이 표시됐다. 부서 상세는 기본 정보/구성원/폴더 권한/비서 연결의 단일 모달로 옮기고 별도 폴더 모달 컴포넌트를 제거했다.
- 세션 실측/개선: 1440×1100 운영 화면에서 본문 높이 990px, 목록 10개, 더 보기 y=654로 아래가 비었다. 이제 실제 행 높이·간격·남은 높이를 ResizeObserver로 측정해 약 1.5화면을 표시한다. 스크롤 자체로 표시량은 늘지 않고 더 보기는 명시 동작으로 유지한다. 필터 변경 뒤 늦은 더 보기 응답이 이전 섹션을 펼치지 않도록 기존 요청 수명 소유자에서 막는다.
- 브랜드 정리: 초기 연결·페이지 로딩·답변 준비를 한국어 텍스트와 중립 진행 표시로 바꾸고 환영 화면, 설정, 메모리, 플러그인 등의 캐릭터를 제거했다. 펫 전용 설정·앱 진입 연결·테스트를 제거하되 파일 첨부·실제 연결 회복 검증은 유지했다. 내부 식별자·라이선스는 유지한다.
- c7e83cc753b 검사: 서버/UI 테스트, 타입, i18n, dead export 통과. folders-page의 불필요한 keys 배열 복사 1건이 lint 실패 원인이라 직접 반복으로 수정했다. 추가 요청과 함께 Actions 및 운영 검증한다.
- 추가 요청 Actions(`07366fa095b`): 이미지 빌드 34734061190 성공(image ID `sha256:cdabd0ae052a4345abe30243e7bbfcb29b4c961fbe27035120a3c24b774a0e73`, manifest `sha256:9dbaeb581ad874962a0641746ef51a94f11ce3bb71a39f8c48ec595659e8256f`). 서버 테스트 통과, UI 28파일 960건 중 948 통과·12 실패. 기존 공유 토큰 로그인 fixture의 신원 조회 완료 상태 누락 3건, 브랜드/테마 기대값, 응답 준비 노드 선택·초기화 대기 문제를 소유 코드와 대조해 수정했다. 인증 우회나 runtime 정책 변경은 없다.
- 세션 추가 조회 보완: 정상 목록 append도 child revision을 올리므로 해당 세대만으로 완료 콜백을 차단하면 정상 더 보기가 확장되지 않는다. 필터·계정 전환이 교체하는 표시 map과 연결 세대로 소유권을 확인한다. 세션 페이지 검증을 독립 test 진입점으로 옮겨 포크 Actions 파일 선택에서 빠지지 않게 했다.
- c7e83cc753b 운영 확인: 안내 503 뒤 193.5초에 원래 주소 자동 복귀, 실행 image ID `sha256:225a22a5f936879762d19dd6a2119cccd033476d49b923bbef99f9c292d5b5ce`, manifest `sha256:7ff75f8bfe21fc833ed6dded8c07629823a80aea8fe49d631465f613fb3ec126`. 모든 서비스 정상. 구형 compose는 다시 응답 대기 실패로 기록했지만 실행 이미지는 교체되고 healthy가 됐다. AC의 배포 결과 기록/실행 이미지 판정 후속은 그대로 남긴다.
- 추가 요청 운영 실측(`07366fa095b`): 안내 503에서 253.7초 후 원래 주소로 자동 복귀. 개인별 대상 8명 모두 기본 표시. 1440×1100에서는 현재 표시 대상 15개 전부 표시(측정 기준 38개, 더 보기 없음), 1440×500에서는 10개와 y=561의 더 보기로 본문을 스크롤한 뒤 접근한다. 스크롤만으로 개수 유지, 클릭하면 15개로 확장했다. 부서 구성원 4명, 이름 초안의 탭 전환/닫기 취소 보존, 고정 부서 폴더 1행, 모바일 390×844 전체 화면과 가로 넘침 없음, 종료 포커스 복귀를 확인했다. 초기 로딩 텍스트 관측, 홈·설정의 캐릭터 생성 0건, 페이지 오류 0건.
- 정적 검사(`07366fa095b`): 코어·코어 테스트·UI·test root 타입, 번역, 포맷, 경계 가드 통과. 캐릭터 제거로 연결이 끊긴 export 4건에서 중단됐다. 사용하지 않는 캐릭터/캔버스/도감 페이지와 전용 E2E·팔레트·미리듣기를 삭제하고 남은 내부 타입은 export를 제거했다. 새로운 제외 규칙은 추가하지 않았다.
- 마지막 코드 `e6c6b987317`: 회사 소개 문구는 실제 범위를 과장하지 않도록 '회사 업무와 자료 관리를 돕는 AI 비서'로 정리했다. [검사 34735175080](https://github.com/necromman/openclaw/actions/runs/34735175080)의 서버 테스트·UI 29파일 981건 통과. 여기에는 새 세션 페이지 진입점의 21건도 포함한다. [이미지 빌드 34735175075](https://github.com/necromman/openclaw/actions/runs/34735175075) 성공, 산출 image ID `sha256:a2dae0a2cf3e8bd27768772bf1d6930f5a5c08a055027b8054be94317582f4e8`, manifest `sha256:a074d71b053ac712101d4ec166998b90e0a873efc2c7fa21b04ff9710cc66ff5`. 정적 검사와 이 이미지의 운영 확인은 후속 기록한다.
- 최종 기능 생산 LOC(8dabc523c8e 대비): +1336/-1695, 순감소 359줄. 테스트/지원 코드 +473/-1189, 순감소 716줄. 번역은 별도 +63/-4. 사용자/부서의 공통 모달과 폴더 편집 재사용, 실제 행 측정·요청 수명 보호를 추가하고 캐릭터·별도 부서 폴더 모달·전용 시험의 중복 경로를 제거했다. 새 DB·설정·권한 정책은 없다.
- 최종 정적 검사 통과: `e6c6b987317`의 chris-check 전체가 성공했다. 변경분 검사, 코어 타입/테스트 타입, dead export 세 레인 0건, 런타임 import cycle 0건과 저장소 가드가 통과했다. 이전 실패/취소 기록과 구분하며, lint 범위는 해당 푸시의 변경 파일이다.
- 최종 운영 확인(12:43 KST): 실행 image ID와 위 Actions 산출물이 일치한다. gateway·IX-Auth·DB·maintenance healthy, cloudflared running. HTML 503 안내 / API GET·POST JSON 503 / 안내 정적 파일 200, 238.9초 뒤 원래 경로·쿼리·해시로 자동 복귀했다. 기존 compose의 응답 대기 실패 기록과 실제 교체 성공은 구분하며, 재기동을 반복하지 않았다.
- 최종 화면 확인: 1440×1100에서는 세션 15개 전부와 더 보기 없음(당시 측정 기준 34개), 1440×500에서는 10개와 y=654의 더 보기, 스크롤 후 클릭하면 15개. 개인별 8명 기본 표시, 부서 4명과 모달 초안/탭/포커스 보호, 모바일 390×844·문서 폭 390, 캐릭터 생성 0건·한국어 로딩 안내·페이지 오류 0건. 설치형 앱 소개도 최종 한국어 문구임을 확인했다. 사용자 저장 검증의 시험 역할·부서·개인 규칙은 원복했고 기존 규칙 32개를 유지한다.
- 증적은 private chris-server `analysis/2026-09-13-user-permissions-modal/`에 전후 화면, 측정 JSON, 권한 저장·원복, 자동 복귀와 실제 실행 이미지, Actions 로그로 보관한다. 초기 로그인 보조 스크립트는 첫 트리가 생긴 즉시 캡처하므로 후속 규칙/정리 조회가 완료되기 전 문구도 포함한다. 완료 판정은 별도의 안정 상태 UI 흐름 결과를 사용했다. 초기 연쇄 조회 중 정리 영역의 미조회 상태를 실패처럼 표시하는 문구는 후속 상태 구분 개선 대상으로 기록한다.

### AE. 대표 계정과 관리자 대리 접속 (2026-09-13)

- 사용자 요청: 대표 계정을 이메일 인증 완료·슈퍼관리자로 준비하고, 본인이 비밀번호를 설정할 수 있어야 한다. 관리자와 슈퍼관리자 모두 사용자 대리 접속으로 실제 화면을 시험한다.
- 기존 계약 조사: IX-Auth의 `POST /admin/users/{id}/impersonate`와 JWT `act`를 재사용한다. 대리 권한·고정 수명·서버 세션의 실제 관리자 필드는 이미 있다. 새 DB 스키마나 설정은 추가하지 않는다. 대상의 역할을 체험하되 대리 중 사용자·시스템 관리 및 본인 보안 변경은 허용하지 않는다. 대리 접속은 슈퍼관리자 대상도 허용하지만 실제 계정 변경의 기존 역할 보호는 유지한다.
- 운영 사전 확인: 대표 이메일의 기존 계정은 0개, 앱의 대리 시작 요청은 404였다. 기존 초대 계약으로 초기 등록했고 ACTIVE·SUPERADMIN·emailVerified=true·활성 세션 0개를 확인했다. 초기 비밀번호는 무작위로 생성해 보관·출력하지 않았으며, 대표가 직접 새 비밀번호를 선택하는 일회용 초대 링크를 재발급했다. 실제 비밀번호 설정 화면의 이름/비밀번호/확인 입력란을 확인했고 새 링크는 소비하지 않았다. 이메일·초대 링크·운영 증적은 private 영역에만 둔다.
- 구현과 자동 검사를 완료했다. 2026-09-13 사용자 최신 지시에 따라 운영 접속 시험은 페이블이 이어서 수행한다. 아래 자동 검사·배포 관찰과 미실행 시험을 구분한다.
- 연결 권한 보완: 대리 중 서버의 관리자/승인/페어링 범위를 제한하고, 일반 읽기·쓰기 범위에 등록된 폴더 규칙·계정 연결·시스템 변경도 해당 관리 경계에서 거절한다. 도구를 거친 팀 비밀 삭제는 기존 요청자의 관리자 권한을 확인한다. 일반 IX-Auth HTTP 읽기도 기존 역할 상한을 적용하도록 누락 분기를 보완했다.
- 감사와 수명: 기존 상세 JSON에 실제 대리 관리자 식별자를 보존하고, 연결은 해당 로그인 세션 단위로만 종료한다. 연결 검증 중 폐기된 세션의 재부착과 동시 갱신 토큰 회전을 같은 인증 소유자에서 보완한다. 첨부파일의 티켓 전용 거절 기록은 기존에 프로필 ID만 전달하는 경로이므로, 티켓에 대리 귀속을 연결하는 작업은 별도 계약 검토가 필요한 후속 항목이다.
- 추가 보호: ADMIN의 슈퍼관리자 초대·비밀번호 링크 발급과 캐시 조회를 기존 역할 경계에서 차단했다. 원관리자 비활성화·역할·부서 변경은 기존 공통 세션 종료 경로에서 그 actor의 대리 세션도 폐기하며 대상 본인의 세션은 보존한다. 대리 중 로그아웃은 복귀용 원관리자 세션까지 종료한다.
- 검증 중 발견한 문제: 첫 Actions에서 자동 포맷으로 이동한 기존 SAFETY 주석이 단언 가드와 충돌했다. 해당 응답 변환을 공통 asOptionalRecord로 정리했다. 새 WS 회귀 시험은 Origin 헤더가 빠져 연결 전 거절된 뒤 대기를 끝내지 못했다. 실제 UI 출처를 넣고 기존 빠른 실패 대기로 교체했다. 제품 동작 실패와 시험 준비 실패를 구분하며 최종 실행 결과는 아래에 기록한다.
- 구현 LOC(e9465ae1557, b7d6327d3ba 대비): 생산 +1320/-263, 순증 1057줄. 시험 +1271/-41, 순증 1230줄. 번역은 별도 +28/-0. 새 대리 시작·복귀·만료/로그아웃 경계, 브라우저 배너, actor 감사와 정확한 세션 폐기가 신규 기능 증가의 이유다. 기존 로그인 쿠키 작성 코드는 공통 소유자로 이동해 중복을 없앴다. 저장 스키마·환경 변수·제품 설정은 추가하지 않았다.
- 연결 실패 시험의 추가 수정: HTTP 503 빈 객체는 기존 신원 오류 분류에서 unknown→400으로 변환된다. 새 시험은 실제 fetch 연결 실패를 모의해 identity_unavailable·503·링크 비노출을 검증한다. 분류되지 않은 신원 서버 HTTP 5xx를 요청 오류 400으로 전달하는 기존 공통 매핑은 명시적 후속 개선 항목이다.
- 추가 검증 보완: HTTP 권한 시험의 IncomingMessage는 기존 실제 요청 fixture를 확장하도록 고쳤고, chat.send 허용 시험은 실제 대화 식별자와 최소 저장소를 제공한다. 초기 연결 대기에도 만료된 대리 세션의 복귀 배너를 노출한다. 별도 터미널·데스크톱·대시보드 문서의 공통 대리 배너는 일반 앱 셸과 다른 렌더 경로이므로 후속 UI 통합 항목으로 남긴다. 서버의 대리 권한 제한은 문서 종류와 무관하게 적용된다.
- 최종 자동 검사: 59bd5fdf1e1의 [수동 검사 34739141467](https://github.com/necromman/openclaw/actions/runs/34739141467) 정적·Node·UI 세 작업 성공. 배포 코드 25a9740e718의 Node 78파일/1444건(게이트웨이 46파일/1141건), UI 30파일/990건 통과를 확인했다. 같은 생산 코드의 core tsgo는 34738824418에서 통과했고, 최신 검사는 관련 게이트웨이 테스트 타입·변경 테스트 린트·전체 dead export 0건을 확인했다. UI 전용 tsgo와 최초 AE 생산 변경 전체 린트는 완료 증거가 없으며 로컬 Vitest/check도 실행하지 않았다.
- 이미지: [34738824386](https://github.com/necromman/openclaw/actions/runs/34738824386) 성공, sha-25a9740e718a, manifest sha256:cde4b26708ac1bb3342f523b38de89672fcf8a46aa31de68e69c8e713fcaed6b. 59bd5fdf1e1까지 후속 변경은 시험뿐이므로 배포 생산 코드와 동일하다. NAS가 14:00:04 KST 같은 manifest로 반영을 시작했다. 실제 실행 컨테이너 확인은 아래에 기록한다.
- 운영 반영 관찰: 14:09:30 KST HTML 한국어 안내 503, API GET/POST JSON 503, 안내 CSS/JS 200. 217.4초 후 원래 경로·쿼리·해시로 HTTP 200 자동 복귀했고 모바일 가로 넘침은 없었다. 새 실행 image ID sha256:406d54ef11d29367543cbf804c1f6ac7882c63a6ab4ad89a7505573176734a8e와 Actions manifest 일치를 확인했다. 14:13 Docker health는 초기 기동 실패 누적으로 unhealthy였으므로 최종 health 재확인이 필요하다. 이 HTTP 복귀를 로그인·대리 기능의 실측 완료로 간주하지 않는다.
- 페이블 인수인계: 사용자 지시로 대리 접속 실측을 넘겼다. 대표님 대리 시작/실제 채팅/원관리자 복귀, 임시 ADMIN 생성·대리·비활성 시 회수, 초대 링크 최종 재발급 스크립트는 준비만 했고 실행하지 않았다. 비공개 FABLE-AE-HANDOFF 문서에 순차 실행·정리·실패 판정·완료 기준을 기록했다. 최초 계정 생성 스크립트는 재실행하지 않는다.
- 인계 직전 재확인(14:14:24 KST): 같은 새 image ID와 manifest, gateway·IX-Auth·DB·maintenance healthy, cloudflared running. 초기 기동의 unhealthy 표시는 해소됐으며 별도 재기동은 하지 않았다. 배포 완료와 접속 시험 미실행을 구분해 페이블에게 인계한다.
- 페이블 운영 실측(14:17~14:28 KST): 런타임 재확인은 같은 image ID·manifest·전부 healthy. SUPERADMIN이 대표 계정(ID 9)을 UI 대리하기로 시작해 `/auth/me`의 대상 이메일과 `impersonatedBy`가 원관리자로 찍힘을 확인했다. CSRF 없는 시작 403, 대리 중 `/auth/admin/users` 403, 중첩 대리 403, WS `models.list`·`config.get` 허용, `config.patch`(missing scope operator.admin)·`folders.rules.list`(대리 중 관리 거절 메시지) 거절을 확인했다. 대리 상태로 실제 `chat.send`가 접수돼 대표 대화에 시험 메시지 한 건이 남았다. 시험 스크립트 쪽 가정 오류 두 건(상세 다이얼로그 기본 탭이 역할·부서라 계정 탭 클릭 필요, `config.get`을 거절로 가정)은 스크립트를 고쳐 재실행했고 제품 결함이 아니다.
- 실측 중 발견한 운영 문제(제품 코드 아님): chat.send 접수 직후 게이트웨이 이벤트 루프가 108초, 이어 14:26에 51초 멈췄다. 게이트웨이 로그는 `slow SQLite transaction hold`(임계 1초) 7회와 `liveness heartbeat delayed` 2회를 남겼고, 그 사이 대표 대리 WS 연결이 tick timeout으로 끊겨 채팅 final 대기·모바일 캡처·복귀 단계는 스크립트 타임아웃으로 미완이다(대리 종료는 finally 로 정상 수행). 같은 시각 관리자 화면의 표시 이름 저장 모달이 멈추는 현상을 사용자가 보고했는데, 저장 자체는 반영됐다(현재 표시 이름 "이화정 대표"). NAS는 2코어에 부하 평균 약 5, Synology `synoimgbkptool` 이미지 백업이 9시간 넘게 CPU 44%를 점유 중이었다. 정지 후 관리 API는 0.1~0.3초로 정상이다. 후속: 백업 작업 시간대 조정 또는 컨테이너 CPU 예약, 게이트웨이 SQLite 쓰기의 동기 구간 점검.
- 임시 ADMIN 대리·권한 회수 시험(C)은 사용자 세션 정리 지시로 실행하지 않았다. 대표 비밀번호 설정 링크는 14:25 KST 최종 재발급했고(이전 링크 무효화), 로그인하지 않은 새 브라우저에서 표시 이름·새 비밀번호·확인 입력 폼만 확인하고 제출하지 않았다. 링크 원문은 비공개 로컬 파일에만 있다.

### AF. 대상별 폴더 권한 화면 (2026-09-13)

- 사용자 요청: 폴더 권한을 폴더별로만 설정할 수 있어 불편하다. 부서·역할·개인을 먼저 고르면 접근 가능한 전체 폴더 목록을 끌어와 폴더마다 숨김·읽기·쓰기를 지정하고 저장할 수 있어야 한다.
- 조사 결과: 서버에 대상 기준 조회와 일괄 저장이 없었다. 기존 `folders.tree.list` 의 미리보기는 숨김 폴더를 제거하므로 편집용으로 쓸 수 없고, 폴더 3만 7천 개 규모에서 화면이 폴더별 호출을 반복하는 방식은 쓸 수 없다.
- 서버: `folders.subject.tree`(대상 하나 기준 한 층, 숨김 폴더 포함, 항목마다 유효 권한·자체 규칙·상속 근거 경로)와 `folders.rules.setMany`(대상 하나의 규칙을 최대 200개 폴더에 한 번에 저장, null 은 삭제, 항목별 성공 여부)를 새 파일 `folder-rules-subject.ts` 에 추가했다. 인가는 기존 관리자 등급 게이트와 감사 기록을 그대로 쓰고, 대리 접속 중에는 두 메서드 모두 거절한다.
- 화면: 폴더 권한 라우트가 새 래퍼를 띄우고 상단 "폴더별 | 대상별" 탭으로 기존 화면과 새 화면을 전환한다. 대상별 화면은 부서·역할·개인 탭과 검색으로 대상을 고른 뒤 루트부터 한 층씩 펼치는 폴더 목록을 보여 주고, 행마다 유효 권한 칩·상속 근거·숨김·읽기·쓰기 버튼·하위 폴더 적용·규칙 삭제가 있다. 변경 행 수를 고정 바에 표시하고 한 번에 저장한다. 기존 `folders-page.ts` 는 수정하지 않았다.
- 검증: tsgo(core·ui·테스트 샤드), oxlint, stylelint, i18n verify, oxfmt 통과. vitest 는 Windows 규칙으로 실행하지 않았고 새 시험 20건은 CI 와 운영 실측으로 확인한다. 전환 상태는 URL 에 남지 않아 새로고침하면 폴더별로 돌아간다.
- 배포·실측(2026-09-13): 이미지 [34741658671](https://github.com/necromman/openclaw/actions/runs/34741658671) 성공, NAS 가 15:10 KST 내려받기를 시작해 15:22 새 이미지(manifest sha256:d4f083fe4b9f4424ffc57c8fe0c851d24915e01b82ddddbbd4d2935abe798b99)로 healthy 복귀. 운영 화면에서 폴더별·대상별 전환, 역할 "직원" 선택 시 최상위 11개 폴더가 유효 권한 칩·상속 문구와 함께 표시되고 390px 에서 가로 넘침 없음(scrollWidth 390), 페이지 오류 0건을 확인했다. 규칙이 효력을 갖지 않는 "시스템 관리자" 역할로 qa 폴더 읽기 규칙을 `folders.rules.setMany` 로 저장했다가 삭제하는 왕복이 서버 결과 ok 와 화면 갱신까지 정상이었다. 운영 규칙은 실측 전 상태 그대로다.

### AG. 부서 코드 자동 생성 (2026-09-13)

- 사용자 요청: 부서 추가 시 이름만 입력하면 짧은 코드가 자동으로 생성돼야 한다.
- 구현: 화면 전용 변경. 새 유틸이 한글 이름을 초성·중성·종성 표기로 로마자화(연구개발 → yeongugaebal, 품질 → pumjil)한 뒤 서버와 같은 정규식(소문자·숫자·하이픈, 32자)에 맞춰 정리하고, 이미 불러온 부서·고아 목록과 겹치면 -2, -3 접미를 붙인다. 코드 칸은 기본으로 접혀 있고 "코드 직접 지정"을 펼쳐 수정하면 자동 갱신을 멈춘다. 이름 아래에 그룹 코드 미리보기와 형식 오류 문구를 표시한다. 서버·IX-Auth 는 바꾸지 않았다.
- 검증: tsgo(ui·테스트 샤드), oxlint, oxfmt 통과. 로마자화 기대값은 Node 로 직접 실측했고 vitest 는 CI 에서 돈다.
- 배포·실측(18:09 KST): NAS 가 manifest sha256:04ce586253d67c294aa2c1627e5ef1e316ebb38ea6eaa96ca6f7733bc09cc4e7 로 교체돼 healthy. 운영 부서 화면에서 이름 "경영지원" 입력 시 그룹 코드 미리보기 dept-gyeongyeongjiwon 이 표시되고 코드 칸은 "짧은 코드를 직접 지정" 링크 뒤에 접혀 있음을 확인했다. 부서는 만들지 않았다.

### AH. 직책 분류 (2026-09-13)

- 사용자 요청: 역할도 부서처럼 화면에서 관리하고 싶다. 조사 결과 5개 역할은 IX-Auth 시스템 역할(삭제 불가)이며 게이트웨이 권한 등급·관리 자격이 그 코드에 고정돼 있어, 사용자 결정으로 권한 등급은 그대로 두고 별도 "직책" 분류를 추가했다.
- 구조: 부서(dept-)와 대칭인 IX-Auth 그룹 `title-<slug>`. 설정 `gateway.auth.ixAuth.titleGroupPrefix`(기본 title-, 환경 변수 없음). 로그인 시 그룹 클레임에서 직책을 읽어 principal·`/auth/me`·연결 신원에 싣고, 기능 지역 테이블 `titles`·`title_members` 에 투영한다. 직책 자체는 어떤 권한도 주지 않고 폴더 규칙 대상으로만 쓰인다.
- 서버: `/auth/admin/titles` GET/POST/PATCH/DELETE 와 `PUT /auth/admin/users/{id}/titles`. 부서 라우트와 공통 안전장치(코드 정규식, 중복 409, 구성원 있으면 삭제 거부, 자기 자신·슈퍼관리자 대상 보호)를 `ix-auth-admin-group-helpers.ts` 로 뽑아 둘이 같이 쓴다. 감사 액션 title-create·title-rename·title-delete·user-titles.
- 폴더 규칙: 대상 종류에 `title` 추가, 우선순위 role < title < department < user. 여러 직책이 겹치면 더 좁은 권한. `folders.subjects.list` 가 직책 목록을 내고 삭제된 직책의 규칙은 고아로 남긴다.
- 화면: 설정 → 직책 페이지(이름만 입력, 코드 자동 생성, 이름 변경, 삭제, 구성원 수), 사용자 상세 역할·부서 탭의 직책 다중 선택, 폴더별·대상별 패널의 "직책별" 탭, 사용자 상세 폴더 권한 대상 선택에 직책 항목. 초대 화면은 범위 밖.
- 검증: tsgo(core·ui·테스트 샤드), oxlint(변경 파일), oxfmt, i18n verify, 설정 문서 기준선 통과. 새 시험(정책 3건, 직책 HTTP, 직책 페이지, 대상별 패널 직책 탭)은 CI 에서 돈다. 운영 실측은 배포 후 아래에 기록한다.
- 배포·실측(18:47 KST): NAS 가 manifest sha256:23b023e6027ed67cb5469ccf71fe4dbf51c8b7b8f13208faaaa11b73da173736 로 교체돼 healthy. 운영에서 설정 → 직책 화면에 "시험직책"을 만들어 그룹 코드 title-siheomjikchaek 자동 생성·목록·API 응답을 확인했고, 폴더 권한의 폴더별·대상별 패널 양쪽에 "직책별" 탭이 나오며 `folders.subjects.list` 가 그 직책을 반환했다. 대표 계정 상세의 역할·부서 탭에 직책 선택이 표시됐다(지정하지 않음). 확인 뒤 시험 직책을 삭제해 운영에 남기지 않았다. 페이지 오류 0건.
- CI 후속: 직책 커밋의 `check:changed` 는 설정 스키마 변경 뒤 생성 파일(`bundled-channel-config-metadata.generated.ts`) 재생성 누락으로, 부서 코드 커밋은 새 유틸의 테스트 전용 export 4개가 미사용 export 로 걸려 실패했다. 테스트(node·UI)는 직책 커밋에서 모두 통과. 두 지적은 다음 커밋에서 정리한다.

### AI. 재배포 후 세션 유실과 재연결 루프 (2026-09-13)

- 사용자 보고: 배포 뒤 열린 탭이 "오프라인 · 다시 연결 중"에 머물고 새로고침해야 돌아오며, 새로고침하면 로그인 화면이 나온다.
- 원인: 세션 저장소(`ix_auth_login_sessions`, 상태 볼륨의 SQLite)와 IX-Auth 서명 키(PostgreSQL)는 원래 재배포에 살아남는다. 문제는 판정이었다. 재기동 직후 JWKS 캐시가 비어 있어 신원 서버 조회가 실패하면 위조 서명과 같은 사유로 세션을 영구 폐기했고, refresh 중계의 연결 실패도 폐기했으며, `/auth/me` 는 어떤 거절이든 쿠키를 지웠다. 화면은 인증 거절을 복구 불가 오류로 분류하지 않아 무한 재시도했다.
- 수정: 신원 서버 일시 장애(연결 실패·5xx·429·JWKS 조회 실패)는 세션을 유지한 채 `identity_unavailable` 503 으로 답하고 쿠키를 지우지 않는다. WS 연결 거절에도 같은 사유와 재시도 가능 표시를 싣는다. 화면은 거절 시 `/auth/me` 를 한 번 다시 확인해 비인증이면 로그인 게이트로 전환(현재 경로 보존, 로그인 후 복귀)하고, 인증이면 새 토큰으로 재연결하며, 503 은 백오프로 기다린다. 실제 401 은 여전히 폐기한다.
- 검증: tsgo(core·ui·테스트 샤드), oxlint, oxfmt 통과. 새 시험(세션 3건, 재연결 분류 11건)은 CI 에서 돈다. 운영 실측은 아래에 기록한다. JWKS 디스크 캐시와 일반 HTTP API 의 503 매핑은 범위 밖으로 남겼다.

### AJ. 진입 시 테마 깜빡임 (2026-09-13)

- 사용자 보고: 사이트 진입 시 라이트로 그려졌다가 다크로 바뀌는 과정이 보인다.
- 원인: `ui/index.html` 의 첫 그리기용 인라인 스크립트가 `data-theme` 만 붙이고 Web Awesome 토큰이 다크를 고르는 `wa-dark` 클래스를 붙이지 않아, 번들이 적용되기 전(운영 실측 약 60ms, 콜드 로드에서는 더 길다) 모든 표면이 라이트 값이었다. 인라인에 `custom` 테마가 없고 앱에 없는 옛 이름 매핑이 있어 첫 그리기와 앱 결과가 어긋나는 문제도 있었다.
- 수정: 테마 해석을 의존성 없는 `ui/src/app/theme-boot.ts` 로 분리해 앱과 인라인 스크립트가 같은 규칙을 쓰고, 인라인에서 `wa-dark`/`wa-light` 와 `color-scheme` 까지 첫 프레임에 적용한다. 저장값 231개 조합에서 인라인과 모듈 결과가 같음을 테스트로 고정했다. 서버 사용자 설정은 이미 localStorage 로 되쓰기되므로 추가 작업이 없었다. 배포 안내 페이지도 같은 오리진 저장값을 읽어 다크·라이트를 첫 그리기부터 맞추도록 `chris-local/nas/maintenance/` 를 고쳤다. 이 안내 페이지 파일은 NAS 에 별도로 복사해야 반영된다.
- 검증: tsgo(ui), oxlint, oxfmt, UI 빌드 통과. vitest 는 CI 에서 돈다.
- 재진단(20:04 KST, 운영 세션 DB 실측): 첫 수정 배포 뒤에도 열린 탭이 비인증이 됐다. 세션 DB 의 모든 행이 `last_seen_at == created_at` 이었고 폐기 사유는 `idle-expired`(로그인 19:26:15, 유휴 만료 19:56:15, 게이트웨이 기동 19:56:24, 폐기 19:56:28). 유휴 창을 미는 호출이 `POST /auth/refresh` 뿐인데 화면이 그 경로를 부르지 않아, 활동과 무관하게 로그인 30분 뒤 세션이 끝났다. 재배포는 그 사실이 드러나는 계기였을 뿐이다. JWKS·refresh 일시 장애 경로는 이번 사례에서 타지 않았다.
- 2차 수정: 인증된 WS 요청과 `/auth/me` 성공 시 세션의 유휴 창을 밉니다(세션당 60초 스로틀, 절대 만료 상한, 폐기·만료 행은 되살리지 않음). 폐기 경로는 건드리지 않았다. 배포 뒤 기존 탭은 한 번 재로그인이 필요하며, 이후에는 사용 중 세션이 유지되고 재배포에도 살아남는다. 실측 기준은 `last_seen_at > created_at` 행이 생기는지다.
- 2차 수정 실측(20:45~20:50 KST, 새 이미지): 게이트웨이 WS 로 작업한 로그인 세션 2건은 `last_seen_at` 이 생성 시각보다 뒤로 갱신됐다(예: 20:45:32 생성, 20:47:45 갱신). 페이지 이동만 반복해 `/auth/me` 만 부른 세션은 갱신되지 않아 그 경로는 후속 보완 대상이다.
- 운영 시험 대화 세션 정리(20:20~20:55 KST): 관리자 목록에 보이던 세션과 저장소 `session_nodes` 의 사용자 대화 40건을 게이트웨이 `sessions.delete` 로 전부 삭제했다. 삭제 불가로 남은 것은 메인 세션과 자동화 세션 하나뿐이다.
- 3차 수정: 활동 기록의 두 번째 문턱(마지막 갱신 뒤 60초 미만이면 쓰기 생략)이 스로틀과 겹쳐 로그인 뒤 첫 1분의 활동을 어디에도 남기지 않았다. 그 문턱을 제거해 첫 `/auth/me` 부터 바로 기록하고, 세션당 분당 1회 쓰기 스로틀만 남겼다. 격리 재현으로 수정 전 첫 쓰기 66초, 수정 후 즉시임을 확인했다. 배포 뒤 판정 기준은 로그인 후 새로고침 한 번에 `last_seen_at > created_at` 이 되는 것이다.
